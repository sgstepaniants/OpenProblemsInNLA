# NR-03 mechanical repair candidate

This is a source-only repair by the diagnostic integration/proof implementer
`/root/is02_cleanup_referee`, not an independent NR-03 referee approval.
No compiler was run; the live worktree remains at the exact diagnostic commit.
Root review is required before integration or another remote run.

The actual modular run 34775864410 at
`e4c3483974ef960d72b24c61a3d1ea76a006a11d` passed LeanCert, Definitions,
Encoding and FamilyDefs. Index and Core then failed with ordinary elaboration
errors; FamilyIdentities and descendants were correctly skipped. There was no
memory abort or module timeout in this diagnostic.

1. `Index.fin127_sum_split`: after three `Fin.sum_univ_add` rewrites, the raw
   goal has exactly the same summands and association as the target, but still
   displays the definitions of coreIndex/singletonIndex/pairIndex/fourIndex.
   Add `rfl` to close that definitional equality. The pinned Mathlib's additive
   theorem is generated by `@[to_additive] prod_univ_add` at
   `Mathlib/Algebra/BigOperators/Fin.lean:158–160`.
2. `Core.coreW_indicator`: two positive branches report simp recursion. The
   existing simp set contains point equalities `h` and `hk` that express a and
   k through each other's values. Replace those two calls with `simp only`
   using the already-proved disjunction and index condition via `if_pos`.
   This reduces the two conditionals directly and avoids recursive point
   substitution. Leave the successful negative branches unchanged.
3. Two `omega` failures in the same theorem retain uninterpreted Fin constructor
   projections in the printed counterexample. Explicit `change ... at hv` and
   `change ...` expose the natural equalities a.val = 127-k.val and
   k.val = 127-a.val before the existing omega step. This is definitional
   unfolding only; the existing bounds a.val<128 and k.val<64 stay in scope.
4. `Core.coreV_at_rep`, positive branch: the raw target is the definition of
   coreClosed after the complement-dot rewrite. Add `rfl` to close it. Leave
   the successful negative branch and multiplication-commutativity proof alone.

Only three theorem bodies change. All theorem signatures, all 93 named/source
declarations, the ten public exports, every definition, all four newly closed
finite kernel decisions, imports and resource options remain unchanged. The
finite FamilyIdentities module remains uncompiled and is deliberately not
modified. No statement is weakened and no new axiom or placeholder is introduced.

`repair.patch` applies to the existing NR03 development prefix. Exact before
and after modules are retained under `before/` and `after/`; `CHECKS.json`
records file and patch hashes. After root approval, integration must preserve
this phase, refresh SOURCE_INPUTS (including existing workflow/lakefile), and
use the existing single workflow with 30 minutes overall and per-module
-M4096/-j1/120-second bounds. No second job or PR has been launched.
