# MI-04 independent proof-repair addendum

**APPROVE the two source repairs.** The prior complete mathematical source approval extends to the exact 21-file closure recorded in CHECKS.json. This addendum preserves the original review and does not claim repaired compilation or canonical acceptance. I am a nonimplementing MI-04 referee.

I read both complete repaired modules, both exact diffs and the authentic Linux log for run 35048156414 at 39736a2e9db176f6f9551ae39c8194013ac770a7. I independently matched the before files to that immutable Git commit, the runtime receipt and my previous complete-source snapshot. Every packet hash, both after files, all 19 other source files and all 10 frozen inputs match. No declaration header or frozen contract changed.

- **Correction:** one local `change` now spells matrix-vector multiplication as `Matrix.mulVec`. The pinned primary API defines the unavailable scoped `*ᵥ` notation as precisely this function. The actual compiler stopped at that notation; the existing diagonal action proof, trial-vector identity and lower perturbation bound retain their mathematical content.
- **ScaledSymmetry:** the actual elaborated target after `dsimp` and `smul_smul` is `(r * r⁻¹) • T = T` with a real scalar. Removing two inapplicable complex-cast rewrites lets the existing nonzero-real cancellation and `one_smul` address this exact goal. The positive scaling argument and universal Hermitian block quantifier are unchanged.

The runtime correctly rejected dependent exports containing elaboration-generated sorryAx. Earlier repaired foundations and Punctured built in this run, but these two repaired modules and the downstream complete graph still require a new actual Linux check. Canonical default-kernel replay, Comparator and negative controls remain pending. The consumed LeanCert quarter-gap certificate and all full-target/fixed-basis/repeated-eigenvalue review conclusions remain unchanged.

No Lean or Lake was run locally, and no author file, repository status or verified count was changed.
