# MF-22 independent root review of the run 35048156414 repairs

**Approve the exact two proof-only edits at source level. Actual repaired compilation and canonical verification remain pending.** Reviewer: OpenAI Codex agent `/root`, a nonimplementing MF-22 referee. This addendum extends the previously completed full mathematical-source review and its earlier repair addenda; it does not claim a new successful runtime.

I read both complete changed modules, both exact diffs, the actual ProjectorRank diagnostic, the relevant pinned primary API definitions and the predictive preflight. I personally authenticated all 862 Git inputs, nine command logs, source pre/post identity and dependency pins of failed run 35048156414 at commit 39736a2e9db176f6f9551ae39c8194013ac770a7. Its prior namespace repair now permits the eigenspace-dimension helper to report only the standard axioms, but the matrix-vector notation failure still blocks the complete module and dependent final proof. The failed run remains failed.

The ProjectorRank edit replaces `A *ᵥ v` by `Matrix.mulVec A v` in one change tactic. The pinned Matrix/Mul definition and scoped notation declaration make that expansion explicit. The argument still places every projector column in the actual simple-root eigenspace, uses its proved dimension at most one and derives all two-by-two minors and the same boundary sandwich. It adds no nonzero-column, eigenbasis or boundary-entry assumption. All declaration types are unchanged.

The second edit is predictive: SpectralTailBounds did not compile in that run because its dependency was blocked. Pinned `to_additive` source gives add_le_add_right a fixed left summand, whereas this calculation needs its final norm on the right. The replacement `add_le_add (spectralNorm_add _ _) (le_refl _)` proves exactly that step of the triangle bound. The earlier authentic MI04 additive-orientation diagnostic supports the API interpretation, but is not an observed MF22 tail error. Constants, roots, all natural powers and the entire final bound remain unchanged. I checked the full changed body and found no mathematical correction needed.

The possible missing scalar_eq_smul_one name required no edit: it is the actual project helper already compiled in TransferCertificates and transitively imported. Dominant, GreenEntryBounds and Conditioning remain byte-identical to the prior reviewed inputs. The 27 remaining closure files, all ten frozen boundary files and all 22 advertised contracts are unchanged. Exact immutable before/after packets and primary API hashes are retained. No Lean/Lake process ran locally, no author source was edited by this reviewer and no verified count or canonical status is changed. Both repaired files require actual Linux acceptance, followed by the full Comparator, kernel, axiom and control gates.
