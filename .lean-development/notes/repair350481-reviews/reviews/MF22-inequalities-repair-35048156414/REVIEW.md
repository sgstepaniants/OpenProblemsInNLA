# MF-22 independent observed and predictive repair addendum

**APPROVE these two exact proof-body edits at source level.** This extends my complete mathematical source review and its retained repair addenda. Reviewer: OpenAI Codex agent `/root/next_inequalities`, a non-implementing MF-22 referee. No new mathematical or scope correction is requested. The repaired complete graph and all canonical gates remain pending.

Actual run **35048156414**, commit `39736a2e9db176f6f9551ae39c8194013ac770a7`, failed. I read the genuine MF-22 diagnostics and bound their full retained log to the parent-authenticated root audit `657e29136ffb96dd00018139a31a363ecd0eb0e21dca9482b8ec3c960e487ef6`. I independently compared both before-files with the exact Git inputs and receipt, then checked the final 29-file closure and all ten frozen boundary files. The parent audit covers all 862 run inputs; this addendum does not claim to repeat that entire operational audit.

The **observed** ProjectorRank error is the unavailable scoped matrix-vector notation in one `change` command. The repaired command spells out `Matrix.mulVec A (fun row => P row j)`. The pinned Mathlib definition declares `*ᵥ` as precisely `Matrix.mulVec`, so the correction preserves the actual matrix action and all theorem headers. I reread the entire module: each projector column belongs to the genuine eigenspace; characteristic multiplicity one bounds its dimension; the finrank characterization gives vanishing minors without a nonzero-column assumption. These minors establish the literal boundary outer-product sandwich. The already compiled eigenspace-dimension component is real component evidence, while the failed notation caused the subsequent `sorryAx` trust rejections. Those rejections are not accepted proofs.

The **predictive** SpectralTailBounds correction changes only `add_le_add_right (spectralNorm_add _ _) _` to `add_le_add (spectralNorm_add _ _) (le_refl _)`. I checked the pinned primary `to_additive` source and the earlier genuine MI-04 orientation diagnostic. The explicit new term adds the fixed third norm on the required right side. This tail was blocked by ProjectorRank in the observed run; it has no actual failure diagnostic or successful execution yet. I reread its complete proof: the three nondominant root powers are bounded by one, genuine operator-norm homogeneity and the triangle inequality bound their sum, and the actual boundary-entry estimate feeds the existing scalar denominator theorem. No constant, quantifier or hypothesis is changed.

The suspected `scalar_eq_smul_one` issue requires **no edit**. It is the existing local `NLA.MF22` helper in TransferCertificates, imported by Dominant, and its unchanged source matches the actual successfully compiled input. I reread Dominant's full reciprocal-root adjugate, two-sided eigenmatrix and cyclic-trace argument. It still derives the nonzero boundary scalar from numerator/denominator coprimality and the genuine rank-one sandwich, including the exceptional parameter case; it does not infer this from idempotence alone.

Exactly these two source files differ from my previous approved 29-file candidate. Their substitutions occur only in proof bodies. All 22 frozen main contracts, original problem definitions, genuine norms, ten frozen files, original mathematical attribution and George Stepaniants's formalization credit remain unchanged. The full target continues to cover every positive parameter, including `rho^2 = 10`, and yields the original existential polynomial condition-number bound with exponent 2, without claiming the stronger exponent 1 manuscript estimate.

`CHECKS.json` records exact inputs and independent checks; `inputs` retains both separate packets, current full source, frozen boundary, primary API files, actual log/receipt/root audit and prior reports. I made no author-source edit, local Lean/Lake execution, publication or count change. Final acceptance still requires successful execution of the repaired complete graph, source-matched canonical Comparator/default-kernel and axiom checks, and rejection/sandbox controls.
