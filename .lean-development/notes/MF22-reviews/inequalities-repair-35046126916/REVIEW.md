# MF-22 independent eigenspace repair reconciliation

**APPROVE the exact three namespace corrections.** This extends my complete mathematical source approval and the previous two-file repair addendum. Reviewer: OpenAI Codex agent `/root/next_inequalities`, a non-implementing MF-22 referee. Repaired actual compilation and all canonical gates remain pending; run 35046126916 remains a failed run.

I read the complete before/after ProjectorRank module, the exact diff, actual run diagnostics and pinned primary Mathlib definitions. The actual errors are all three failed `.eigenspace` lookups on `Matrix.toLin'` expressions, followed by dependency/trust failures. The replacement explicitly selects `Module.End.eigenspace` on those same linear endomorphisms and eigenvalues. Its pinned definition is the genuine eigenspace, with membership exactly `A v = z • v`. No surrogate vector space or additional assumption is introduced.

Two occurrences are in auxiliary theorem header expressions, so these are syntactic header changes. The previous headers never elaborated; I make no equality claim between previously elaborated type terms. Their intended mathematical hypotheses and conclusions are unchanged. All 22 frozen main contracts, definitions and ten frozen boundary files are byte-identical.

The substantive proof remains correct: distinct nodal roots have characteristic multiplicity one; geometric multiplicity is bounded by that multiplicity. Every projector column belongs to that eigenspace, so the finrank-at-most-one characterization expresses every column as a scalar multiple of one vector and makes every two-by-two minor zero. This works even for the zero eigenspace or zero columns, with no division by a column or matrix entry. The resulting literal boundary outer-product sandwich follows from these minors, rather than from idempotence alone. The separate nonzero-scalar proof and the full growing-mode cancellation route are unchanged.

`CHECKS.json` binds the proposed 29-file candidate: the other 28 source files match the prior full review with its two already approved proof repairs. The before file also matches the actual Git input at `64a04495cb03fa7496cbdc3dc72f94779ab4c340`. Exact raw diagnostics, source pair, diff and primary API files are retained under `inputs`. I made no proof edits, local Lean execution, publication or verification-count change. Final acceptance requires the actual repaired complete graph and the normal Comparator/default-kernel, axiom, negative-control and source-binding evidence.
