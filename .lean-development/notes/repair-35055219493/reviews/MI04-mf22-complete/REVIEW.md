# MI-04 second independent complete-source review

**Verdict: approve the complete mathematical source, target fidelity, and the two observed Probes repairs. Complete fresh Linux execution and Comparator/default-kernel verification remain pending. This report does not mark MI-04 Lean verified.**

Reviewer: OpenAI Codex AI agent `/root/mf22_publication_referee`, 16 September 2026 UTC. I did not implement or edit MI-04. Root implemented the latest two-line repair, and this is a separate independent review. I applied the repository's scoped Tau Ceti protocol; this is not an official Tau Ceti service result, external human peer review, or a verification of Lean's implementation.

I personally read all 21 current active files in full: 2,168 lines and 92,471 bytes, including Definitions and Solution. I also read all 21 frozen Challenge declarations, the complete original mathematical manuscript, the full canonical problem, numerical obligations, source correspondence, the narrow repair, the primary Lean instance-variable rule, and the complete actual 392-line MI-04 compiler log. Library interfaces were checked against the pinned primary source. Other referees' historical findings were consulted after my complete source reading; their approval is not the basis for mine.

The reviewed closure is `proof-handoffs/repair-35055219493-provenance/FINAL-CLOSURE.json`, SHA-256 `3f553e705896b765090592e3a637fb202cf165bea9dbc01f437d6507298ec3c7`. `ALL-21-SOURCES.json` records every exact source hash; `reviewed-source/` retains the complete reviewed source as text. `CHECKS.json` binds the independently executed audit. No author source, proof, frozen statement, repository status, or count was changed by this review.

## Full original target and scope

The final theorem covers every natural dimension `n >= 1` and every complex square matrix `X`. Its premise quantifies every Hermitian `A,B` for which the actual block matrix `[A X; X* B]` is positive semidefinite. Its conclusion produces an actual Hermitian `K` and complex scalars `alpha,beta` satisfying `X = alpha K + beta I`. Normality, diagonalizability, distinct values, invertibility, positive definiteness, and bounded entries are not added as hypotheses.

The literal `spectralNorm` is the norm of `Matrix.toEuclideanCLM` on complex Euclidean space. The PSD and Hermitian predicates are the genuine Mathlib ones; every coordinate of the `2n` block is retained. The actual complex inner product is used. `topValue` is defined as a supremum over all unit vectors, and its attainment, comparison, scalar-order, positivity, and norm properties are separately proved. No definition assumes the desired theorem. The original manuscript's stronger four-way equivalence includes a converse that this formalization does not claim; the necessity implication formalized here is the complete permanent canonical question.

Zero and scalar matrices, singular completions, repeated eigenvalues, arbitrary eigenspace multiplicity, and dimensions one and two are preserved. In particular, no division by an eigenvalue or spectral separation beyond the deliberately chosen auxiliary peak occurs. The finite nonempty coordinate hypothesis is appropriate to the generic variational helpers and is supplied by `n >= 1` in their uses.

## Complete mathematical review

**Quadratic forms, extrema, and block completions.** `Quadratic` correctly reconciles the two inner-product conventions by taking real parts. Its operator bound is the genuine Cauchy-Schwarz/operator-norm bound. `Rayleigh` proves a maximum on the compact, nonempty unit sphere; a coordinate vector proves nonemptiness. Thus the conditional supremum is used with actual attainment rather than an unstated boundedness convention. Positive quadratic forms remove the absolute value in the standard self-adjoint Rayleigh norm formula. The zero vector is dealt with before normalization. Scalar shifts and the equivalence between scalar PSD order and the top value are valid also at the zero boundary.

`BlockSymmetry` uses opposite block-coordinate tests to prove `c = topValue(-K_X(T)) >= 0`. The completion `c I + K_X(T)` is genuinely PSD, potentially singular, and has diagonal sum `2c I`. Applying the original universal premise yields one inequality between the extrema. The explicit sign unitary `diag(I,-I)` and the test `-T` give the reverse one. This selects useful completions in a proof under the full universal premise; it does not narrow that premise. `ScaledSymmetry` transports every Hermitian test matrix through the inverse unitary or positive scaling, so both invariances have the needed universal scope.

**Exact second-order argument.** `Coordinates` and `Correction` use the correction `w_a=0`, `w_j=V_ja/(1-d_j)`. Each off-peak gap is positive from `d_j <= 1/2`. The exact test-vector squared norm is `1 + epsilon^2 ||w||^2`, and its exact numerator is `1 + epsilon^2 (||w||^2 + L) + epsilon^3 R`. The lower bound retains this denominator and divides only by proved positive quantities. It is not an unnormalized test-vector estimate.

`Punctured` decomposes an arbitrary vector into its peak coordinate and orthogonal remainder. The upper operator bound applies to the remainder, and every mixed term has the correct adjoint, index, and conjugation. `ScalarGeometry.weighted_square_bound` is the nonnegativity of the actual complex square `|z-qw|^2`, with division justified by `q>0`. Under the frozen small-positive-epsilon condition, all modified gaps exceed `1/4`. Summing over every off-peak coordinate, then using the nonnegativity of the coefficient and `|v_a|^2 <= 1`, gives the bound for every unit vector. It therefore bounds the attained maximum, not merely a selected eigenvector. `Limit` proves both bounding expressions tend to `L` on the positive right-neighborhood and squeezes the real quotient. No analytic eigenvalue branch, numerical root, or assumed asymptotic error is used.

**Weighted probes and normality.** The two diagonal peaks of the actual full block pencil in `WeightedProbes` produce row and column magnitudes with denominator `1+d_j`. The distinguished diagonal term has denominator two in both sums and is retained. The negative-diagonal/sign-flip identity and positive scaling equate the quotient functions on the right-neighborhood; its nontrivial filter permits uniqueness of the limit. The base and modified probes in `Probes` differ at only one off-diagonal coordinate, where the weight changes from one to two-thirds. Their difference is exactly one-third of the chosen squared magnitude. Diagonal cases are immediate, and all sums remain symbolic in arbitrary dimension.

`BasisTransport` extends every orthonormal pair to a complete orthonormal basis and builds its actual unitary matrix. The entry identities therefore imply the pair property for arbitrary vectors, not just a coordinate basis. `Normality` includes every term in the Parseval sums, including the equal diagonal term. It compares `Xu` and `X*u`, handles the zero vector separately, and normalizes every other vector using a proved nonzero norm. The primary adjoint-norm criterion establishes normality of the actual continuous linear operator, and the injective matrix star-algebra equivalence returns the literal matrix equation `X*X = XX*`.

**Full diagonalization and the affine line.** `JointBasis` restricts the complete joint-eigenspace decomposition to the finite sets of actual eigenvalues of the commuting self-adjoint real and imaginary parts. An omitted eigenvalue gives a bottom eigenspace, which is explicitly proved. The orthogonal internal direct sum covers the whole space. Its subordinate orthonormal basis includes a full basis of each intersection; it does not choose only one vector per eigenvalue pair. Thus all multiplicities remain. `Diagonalization` recombines the two eigenvector equations to form a full normal-operator eigenbasis and proves the matrix identity by its columns and both unitary inverse equations.

`Geometry` first extends the pair property by homogeneity to all orthogonal vectors, separating zero cases. The sparse vectors with coefficients `(1,1,1)` and `(1,I,-1-I)` are exactly orthogonal. Their two coefficients are `a+Ib` and `a-Ib`; equality of their norm squares gives the correct vanishing oriented area. Repeated indices are handled before this calculation. `ScalarGeometry` treats an entirely coincident spectrum with `alpha=0`; otherwise it proves a chosen difference nonzero before division and proves every quotient is real. `Conclusion` conjugates the resulting real diagonal back to an actual Hermitian matrix and expands the affine representation. The final theorem uses precisely the full original premise and conclusion.

I found no mathematical gap, vacuous additional assumption, hidden conclusion, lost coordinate, or degenerate-case omission in this full route.

## Frozen contracts and the Nonempty correction

All ten frozen inputs match both the immutable freeze and the retained snapshots. All 67 entries of `STATEMENT-SEAL.json` and all eight retained statement-review items are byte-exact. All eight original source files were independently bound to their Git blobs at upstream `ce47b5630bf3680d9211131c3a43825b022c139a`. The canonical ID and target are unchanged.

I compared all 21 implementation headers with their independent Challenge headers, including their enclosing section contexts. Renaming a proof binder from `hM` to `_hM` or `hn` to `_hn` does not alter its type. The substantive previously reported issue was the implicitly included `[Nonempty ι]` in the Challenge's Variational section. The current implementation of `positive_quadratic_iff` explicitly includes that same instance. The retained old and new source differ by only this binder in the predictive correction; its mathematical body is unchanged.

I independently read Lean 4.33.1's primary `Lean/Elab/MutualDef.lean`, lines 420–590. Its `withHeaderSecVars` rule includes available instance-implicit variables whose dependencies are included. Thus the frozen Challenge includes `[Nonempty ι]` even though that proposition can mathematically be proved without it. The implemented binder resolves this formal-fidelity issue while leaving the final `n >= 1` target unchanged. The remaining seven generic variational exports already have the same nonempty context; the other thirteen exports carry their explicit dimension binders. This source comparison and the elaborator rule are evidence for matching, not a replacement for actual Comparator execution. The earlier finding was predictive and must not be relabeled as an observed Comparator rejection.

## Observed Probes repair and actual failed execution

I read the complete 392-line MI-04 log from run `35055219493`, job `104663872288`, at commit `dbc2371b74bcae58f6d6957bd588e32c56c8dc91`. The Ubuntu Linux development build ran as UID 1001. It builds the substantive lower/upper Rayleigh bounds, the limit, and the weighted-magnitude identity with only the three configured foundational axioms. It then fails in `Probes`: one branch has not rewritten `k != i` after `k=j`, and a finite-sum expression leaves the second occurrence of `k` outside its binder. The downstream export acquires `sorryAx` from these failed elaborations, and the existing trust assertion correctly rejects it. This is a real failed build, not accepted mathematical evidence for the complete problem.

The new proof-body repair explicitly substitutes `k` in the equality branch before simplification and parenthesizes the whole subtraction inside the finite sum. Both edits directly address the logged diagnostics. I verified the exact unified diff, unchanged declaration headers, and all 20 other source files unchanged. The new Probes SHA-256 is `0e4b3d9a0e6bb70c8b91a1669e89785cbefe99777315d9a4b52d961fa93f5f76`. It is an appropriate narrow repair, but no successful run of those new bytes is claimed here.

The independent audit binds all 1,454 actual receipt inputs to Git blobs at the failed commit, all seven command-log hashes, every post-command source map, all 21 prior active sources, and the frozen Challenge. It verifies all eight members of the authenticated artifact archive, SHA-256 `a6109b467efcb2848224684f5090be04342d704e962f90cc87207e4dcecb5de1`, and matches all 345 substantive MI-04 log lines to the raw GitHub job log. The root's retained failure audit is corroborated rather than treated as a substitute for these checks. The closure's correction of stale inherited run identifiers changes no source or frozen hash.

## Computation, reuse, attribution, and remaining gates

The proof minimizes numerical work by using symbolic finite sums, exact rational probes, and sparse vectors. It needs neither growing interval subdivisions nor numerical eigenvalues. LeanCert provides one exact kernel-mode certificate `0 < 1/4`; it is actually consumed in both off-peak denominator arguments. All 21 public exports also have kernel trust assertions and transitive axiom-print commands. No native decision procedure, unsafe definition, new axiom, or placeholder occurs in the complete 21-file Solution import closure. Challenge is separate and never imported by the implementation; its 21 deliberate placeholders are specifications only.

I checked the actual pinned Mathlib operator, PSD, Rayleigh, adjoint, real/imaginary-part, joint-eigenspace, finite-eigenvalue, and subordinate-basis APIs. The normal spectral theorem is built from those proved foundations, with its finite-index restriction supplied explicitly. The modular organization and comments explain the new arguments and distinguish them from reused APIs. Cosmetic linter warnings do not justify changing the frozen scope or delaying a narrow correctness repair. The current metadata validates against the repository schema but still describes its historical statement phase; publication metadata must be updated truthfully after actual acceptance while retaining the frozen originals.

Matthew J. Colbrook, Department of Applied Mathematics and Theoretical Physics, University of Cambridge, retains original mathematical authorship. George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology, receives formalization credit and the documented AI-assistance disclosure. No personal formalization-author email is added. Bourin and Lee retain attribution for the conjecture. There is no priority, source-author endorsement, or human peer-review claim.

The canonical Lakefile still defaults to Challenge, although Solution is registered. The fresh candidate must explicitly build Solution, or document a narrowly scoped default-target change while retaining the frozen Lakefile. Before publication or counting, require a successful full exact-source Linux build, all 21 actual Comparator/default-kernel checks with only `propext`, `Classical.choice`, and `Quot.sound`, the required control tests, and independent runtime reviews tied to that success. This report authorizes proceeding to those gates; it does not assert they have happened. I ran no local Lean, Lake, cache, or Comparator process and made no commit, push, publication, or count change.
