# MI-04 independent observed-error repair addendum

Reviewer: OpenAI Codex agent `/root/mi04_independent_referee`, independent of the coordinator's implementation. This extends my complete mathematical source review `98c0f308ffd75008cb32522560cf4427e76244e0fc0f57f7d4cbc43f4b203f9e`, Nonempty addendum `f41287268c032fe57220b1f78e4c8e3d1c0bb0a322dba4b143e3145fd0f4392e`, and authenticated failed-run diagnosis `20ba60651ac23fb21b0e0b708440d60f41e938eb360a6b21b4d0781984511f04`.

**Source verdict: approve both exact Probes proof-body repairs.** The current complete closure is `3f553e705896b765090592e3a637fb202cf165bea9dbc01f437d6507298ec3c7`; use this corrected closure for further preparation. The original repair packet is `1ffeddba9d626de0ec3e0cbf14a4ca591a4c1ca34cff691cf616cc4fc8918d63`, with the append-only provenance correction `ffe7f27c2fdc9214c5d85fbcb9593bf5b2e8ad8b438700cfab1743a5e15ce2fc`. All source approval is conditional on the remaining actual execution gates; it is not a successful-build claim.

I read the entire new Probes file and its complete diff against the source that actually failed in Linux run 35055219493 at `dbc2371b74bcae58f6d6957bd588e32c56c8dc91`. That failed source hash is `659176443980b438e7f55bd8df45ee5174ac87117fc37a73bd6c4900f349df01`; the revised hash is `0e4b3d9a0e6bb70c8b91a1669e89785cbefe99777315d9a4b52d961fa93f5f76`.

The first edit explicitly substitutes k=j in the positive branch of `tweakedPeak_bounds` before `norm_num`. This transports the existing k≠i hypothesis to j≠i as well as the goal, so the peak is exactly 1/2 in that branch. The other branch still gives zero away from i and j. The bound and all parameters are unchanged, including arbitrary finite dimension and coincident i/j in the lemma itself.

The second edit parenthesizes the entire subtraction within the finite-sum body of `weighted_probe_difference`. Both occurrences of k are consequently inside the summation binder. The termwise mathematical identity is unchanged: terms outside j cancel, while the j term is q_j−q_j/(3/2)=q_j/3 because j≠i. No approximation, extra premise or dimension restriction is introduced. The off-diagonal magnitude argument still subtracts the two independently available weighted identities and cancels exactly.

The independent audit verified all 21 current sources against the new snapshots, all 21 before-source hashes against the authenticated Git-bound receipt and all seven command postmaps, the other 20 sources byte-for-byte unchanged, all ten immutable inputs unchanged, and exact whole-file equality to the two specified replacements. Every definition and theorem header in the edited module is identical. Actual logs and receipt in the repair packet match the earlier independently authenticated run bytes.

I found and reported stale `failed_run_id` and `actual_failed_commit` fields in the first new closure. The coordinator preserved that sealed closure and added a superseding record. I verified that the correction changes only those run identifiers and explicit supersession/explanation fields; all source and frozen maps are identical. The current record now correctly identifies run 35055219493 and commit dbc2371b74bcae58f6d6957bd588e32c56c8dc91. This resolves the provenance finding without rewriting history.

The scoped Tau Ceti fidelity/correctness review retains the prior complete mathematical approval. There are no changes to the original all-dimensional complex-matrix target, norm definitions, singular or repeated-spectrum cases, consumed LeanCert certificate, author attribution or permanent problem ID. The Nonempty signature correction remains in place and was accepted by the latest actual build, although full Comparator type equivalence still awaits execution.

Fresh complete Linux elaboration of these revised bytes, transitive axiom audits, actual Comparator and controls, and canonical verification remain pending. No local Lean/Lake, proof edit, preparer update, package creation or publication was performed by this reviewer. No verified count increases from this addendum.
