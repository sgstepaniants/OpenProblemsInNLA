# RA-02 independent repair and source-location continuation

**Approve the two exact proof-body repairs for a fresh Linux prefix run, using
the clarified eleven-file source selector. No new runtime acceptance is claimed.**

Reviewer: `/root/ie13_continuation`, distinct from implementation author
`/root/mf22_publication_referee`. I previously reviewed the mathematical route,
pinned APIs and first six proof modules. I did not author these RA-02 Lean
statements, proofs or repairs. This is a bounded repair continuation; the
separate complete four-module arrowhead reviews remain attributed to root and
the MI04 referee. It is not a review of any newer spectral-prefix implementation.

The current selector packet is
`repair-35090376438-source-location-clarification`, manifest
`d3e9dca135720d0f10bfe20c609ebf6f5ad2f5bf09f0760226689800e4f7565d`,
closure `f9492579c80bbe32fd2eba7ea4d2548c73a48e9135f10e1651c1bb43942ba924`.
It preserves the observed repair and the separate unobserved follow-through
without changing their mathematical bytes. I read both complete replacement
modules, both full patches, all frozen Definitions and 27 Challenge statements,
the entire actual RA-02 module and Challenge logs, the provenance clarification,
my first-prefix review and both complete arrowhead review reports. I inspected
the relevant pinned conditional, function-star, rank-one matrix and PSD APIs.

The actual PivotAlgebra diagnostics leave the matrix-valued conditional
unreduced. The supplied nonzero or zero pivot hypotheses already determine
the relevant branch. Passing those hypotheses with `↓reduceIte` exposes that
same branch before the unchanged field/ring calculation. This pattern occurs
in the pinned Mathlib matrix Transvection proof. It adds no hypothesis and
changes neither the Cholesky update nor its zero-pivot totalization. The two
local `Pi.single` expressions now explicitly have type `Fin n → ℂ`, resolving
the reported dependent-family inference problem while retaining the actual
coordinate row selector. The second occurrence is a disclosed identical-pattern
follow-through within the same lemma, not a separate observed diagnostic.

The congruence argument remains mathematically valid: with
`E = I - e_j row_j(A)/A_jj`, right multiplication gives the genuine rank-one
residual; its pivot row vanishes, so the remaining rank-one term on the left
also vanishes. Thus the residual is exactly `Eᴴ A E`. The actual complex PSD
congruence theorem applies, and a zero pivot returns the original matrix.
No PSD conclusion, positive pivot or normalization law is assumed in a helper.

The StateConstruction amendment uses the same condition reduction only after
proving the distinguished pivot nonzero from the positive corner. Pivot row
and column cancellation are unchanged; the ordinary block still identifies
the exact frozen false state. Its PSD follows from the same congruence theorem,
and active diagonal positivity still uses the positive corner and erasure
formula. This module was absent from run 35090376438: its one-line change is
explicitly an unobserved source follow-through, not an invented compiler error
or a successful compilation claim.

The author originally changed two private active files while retaining before
and after snapshots. Root detected that this invalidated earlier reviews'
current path bindings. The original eleven-file author workspace has now been
restored exactly. I independently verified that restoration, every candidate
location, and all three historical mutable-path observations against their
retained immutable after snapshots. The old records are preserved as historical
observations rather than misrepresented as current file checks. Integration
must select the two replacements from the clarified `SOURCE-LOCATIONS.json`;
the restored author workspace itself contains the original unrepaired files.

The executed static audit passed. It rehashed all five source-packet inventories,
the retained independent review inventories and 288 evidence bindings. All
eleven original and prospective sources reconcile with their closures. Exactly
two existing proof bodies change; all 111 extracted header records, ten
implemented public signatures and nine frozen files remain unchanged. The
equation-style recursive definitions are protected by their complete immutable
bytes, independently of the header extraction heuristic. All 27 contracts,
dependency/build configuration, scopes, imports, options, kernel assertions,
axiom prints and resource limits are preserved. No proof hole, custom axiom,
native bypass, Challenge import or new global assumption was introduced.

All seven before-source files actually included in run **35090376438**, plus
Challenge, match literal `0ce7dc10d8b901ee57e8d226f36a30979bb205aa` Git and the
receipt. All eleven recorded post-command maps equal the initial 1,929-input
map. The actual module command failed in PivotAlgebra; Definitions, Parameters
and Numerical built, with the reported numerical declarations using only the
permitted foundational axioms. Trust assertions rejected the failed module's
`sorryAx` dependencies. The Challenge command exited zero with 27 intentional
specification holes. The complete artifact logs match the raw job log in order.
The parent's broader operational audit is bound, not relabeled as my rerun.
All six cited Mathlib source files match their literal pinned Git objects;
the unused SplitIfs source is retained as investigation provenance, not a new
proof dependency or a claimed implementation technique.

Applicable Tau Ceti correctness, generality, nonvacuity, trust and reuse checks
found no new issue. The target remains the normalized full path law for all
complex PSD matrices and its negative polynomial-factor conclusion with
arbitrary real exponents. This repair introduces no numerical computation,
rank enumeration or interval subdivision. Mathematical credit to Matthew J.
Colbrook, formalization credit to George Stepaniants at the Department of
Computing and Mathematical Sciences, California Institute of Technology,
AI assistance and Apache-2.0 are retained without contact information.

No further source correction is requested for this bounded repair. The
remaining 17 contracts in this eleven-file prefix, actual proof compilation,
final independent full-source reviews, all-export default-kernel/Comparator
and controls, and exact publication evidence remain pending. No local
Lean/Lake/cache execution, source edit, Git mutation, publication or count
change occurred in this review.
