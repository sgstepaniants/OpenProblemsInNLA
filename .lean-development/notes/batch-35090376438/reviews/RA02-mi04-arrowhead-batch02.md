# RA-02 arrowhead batch 02: independent source review

**Approve this exact four-module prefix for a future Linux build.** I found
no mathematical or source-level API correction required. This is source
approval only: ten of the 27 frozen contracts have implementations in the
combined source prefix, and **17 remain unfinished**. No complete RA-02
verification or new runtime acceptance is claimed.

Reviewer: `/root/mi04_independent_referee`, a nonauthor of these Lean files.
I previously reviewed the complete original target, manuscript and statement
boundary. For this continuation I read the current canonical target, all
27 contracts, numerical obligations and source correspondence, all four new
modules, and all seven preceding sources including Definitions. The earlier
complete manuscript review remains bound; the new finite arrowhead route is
an alternative route to its negative conclusion, not a claim to formalize
the manuscript's stronger limiting factor or entrywise-positive witnesses.

The author packet is `arrowhead-batch-02`, manifest
`93725c9353a474ca429b5c8a125dcff15572923070905b647f2b852f1cf1bb56`,
and its complete 11-source closure is
`d830f581852fd1951184bbc70b1a544c751444fc2d885ce284a9a40dfdf53351`.
The prior six proof modules and Definitions remain byte-identical to the
first foundation packet. All nine frozen inputs, 27 contracts, dependency
pins, resources and original problem identity are unchanged.

## Mathematical review

- **ArrowheadEntries:** ordinary labels are the injective `Fin.castSucc`
  image and exclude the distinguished last label. The literal support,
  symmetry and diagonal formulas match the frozen matrix definition. The
  positive corner follows from its positive epsilon power and nonnegative
  finite sum, including an empty active set. Erasing an active label gives
  exactly its removed weighted square. Substitution proves the false-state
  diagonal `d_i*c(U.erase i)/c(U)` with a genuinely nonzero denominator.
- **ArrowheadQuadratic:** the proof expands the actual complex matrix-vector
  product and conjugate quadratic form before identifying it with the real
  finite sum of squares. `stateEnergy` is that explicit scalar expression;
  equality to the original matrix form is proved, not built into an assumed
  PSD definition. Every weight is positive. For the full matrix, a nonzero
  last coordinate gives strict positivity directly; otherwise a nonzero
  ordinary coordinate supplies a positive summand. Thus the actual complex
  matrix is positive definite. The helper reasoning handles rank zero and
  empty sums, while the public contract keeps its exact `1 ≤ r` premise.
- **StateConstruction:** the distinguished-pivot update is the original
  total rank-one Cholesky formula. Its strictly positive corner justifies
  division. Pivot row/column cancellation and the ordinary-entry calculation
  identify it exactly with the frozen false state. PSD follows through the
  earlier genuine complex congruence theorem. Active ordinary diagonals are
  strictly positive in both states, including a singleton active set. No
  Schur-complement positivity or state-transition conclusion is assumed.
- **ProbeValues:** the distinguished coordinate is one, so the probe is
  nonzero. Its ordinary coordinates cancel every weighted square, giving
  exact energy `epsilon^r` and actual squared norm
  `1 + sum_i alpha_i^2 ≥ 1`. This is not yet a statement about eigenvalues:
  the genuine ordered-spectrum and Rayleigh bridges remain explicit work.

The preceding probability definitions still include all ordered histories,
repeated labels and zero-probability events. Conditional masses normalize
over every label; uniform dummy labels only totalize the zero residual.
PSD preservation is proved for the actual update, and no independence
assumption is added. The ultimate target still quantifies over every complex
PSD matrix, exactly r steps and arbitrary real constants C and p. Using real
positive-definite counterexamples does not narrow that universal target.

I checked the relevant pinned signatures for complex norm-square and order,
finite-sum distribution and erasure, `Fin.lastCases`, and the genuine matrix
PSD/PD constructors. The finite-sum names generated by `to_additive` have the
needed orientations. The installed core contains the `if_true` alias used
in the diagonal calculation. Thirteen Mathlib snapshots were independently
matched to their exact pinned Git blobs; the core Fin snapshot was compared
with the installed v4.33.1 source, without claiming new remote core Git
authentication. These checks do not establish successful elaboration.

## Audit and remaining work

The independent Python audit passed 317 checks and retained 120 input
bindings. It rehashed all 29 current and 37 prior inventory entries, all
external author bindings, every source and frozen file; it independently
matched ten public theorem headers to the original 27. Definitions and
Challenge match literal statement commit
`09f9c4fd10c6e7d1efdb60eb0f43bc33ed920833`. The original canonical target and
raw manuscript hashes were also checked against their actual Git objects;
the contact-redacted manuscript copy is separately identified.

No proof hole, custom axiom, native bypass, Challenge import, foreign project
import, resource increase or email was found in the source graph. Kernel
trust assertions and axiom prints are present but were not executed here.
The one existing LeanCert point certificate `exp(1) ≤ 3` is consumed by the
symbolic denominator helper. No new interval computation, rank enumeration,
numerical eigensolve or determinant expansion is introduced. Attribution to
Matthew J. Colbrook's mathematics and George Stepaniants' formalization,
department and university, AI disclosure and Apache license are preserved.

The remaining contracts are `ordered_spectrum`, `least_eigenvalue_rayleigh`,
`spectral_tail_semantics`, `arrowhead_tail`, `residual_state_updates`,
`distinct_history_state`, `distinct_history_identity`, `retained_history_count`,
`retained_prefix_description`, `retained_trace_bound`,
`retained_contribution_bound`, `expectation_lower_bound`,
`binomial_exponential_bound`, `exponential_tail_factor`,
`exponential_dominates_real_power`, `universal_counterexamples`, and
`no_polynomial_trace_factor`.

Applicable Tau Ceti fidelity, nonvacuity and proof-quality criteria informed
this review; no official referee service was run. This review executed no
Lean, Lake, cache, Comparator or controls and changed no author source, Git
state, shared worktree or count. These four modules are excluded from the
already launched foundation-only run. A real build, completion of the
remaining contracts, two complete-source reviews, all-27 Comparator/default
kernel/control checks and exact publication/upstream runs remain required.
