# RA-02 independent Matrix-scope amendment review

Reviewer: `/root/mi04_independent_referee`, a nonauthor of the draft and amendment. **Approve this exact amendment for the next Linux statement-elaboration attempt.** It resolves the identified missing notation scope without changing any definition text, theorem header, mathematical obligation or assumption. This is not successful elaboration, a statement freeze, an implemented proof or verification of RA-02.

The reviewed root-authored packet has manifest `4ba11c4fab5fc0b4ba40192c3993511aecf74129f0211c4588580b498c23fb3e`. I read the full amended 156-line Definitions and 205-line Challenge, the complete two-line patch and amendment record, and the relevant pinned primary Matrix notation declaration. I independently rehashed every one of its seven inventoried files. The before files exactly equal the corresponding files in original draft manifest `10c1c64a95804f21dde488409bdda7affaa7f1d07b3d633b242314468db3cbb6`; all 62 original draft inputs remain unchanged.

The only two source edits append `Matrix` to `open scoped BigOperators ComplexOrder`, once in each file. Every other source byte is identical. The two amended source hashes are Definitions `1a0f251cfcc38f57787de3ee79dc8c89e7bf0f496ebfe9196a66f8b3f452a376` and Challenge `eb95beaddd92fc1b24d2df1d7bc9db9afec7a3391759dd2d613119c5f25d4340`. All 27 declaration names and headers remain unchanged and still match the original Comparator configuration exactly. Definitions has no holes or new axioms; the Challenge still contains exactly its 27 intentional specification placeholders.

The retained `Mathlib/Data/Matrix/Mul.lean` is byte-identical to the blob at Mathlib commit `0df444a360eaa60ab8c11dca51a86af692955474`. Its declaration at line 702 is `scoped infixr:73 " *ᵥ " => Matrix.mulVec`, within namespace `Matrix`. Its actual `mulVec` is the usual row dot product with a column vector. Opening this scope therefore makes the intended matrix-vector notation available at both uses: the actual conjugate quadratic form in Definitions and the actual eigenvector equation in the `ordered_spectrum` contract. The parentheses around the quadratic-form multiplication already disambiguate the expression. No namespace, import, numerical constant, scalar field, probability convention or spectral ordering changes.

My complete original-target statement review, sealed by `e50b2f0de8a5734f19b3dd79e901107fc657b324f43867e8371c05324a9e8fb6`, carries forward by this exact delta. That review covered the full normalized all-history randomized Cholesky law, zero trace and zero pivots, the actual decreasing Hermitian eigenvalue tail, all real polynomial exponents, finite dimensions and ranks, and the symbolic arrowhead counterexample route. None of those conclusions becomes an assumed definition through this amendment. The preserved root statement-review record is also included for integration provenance; it is not represented as a new root review of my report.

The reason is explicitly a source-based observation before RA-02's first compiler attempt. No previous RA-02 compiler failure or successful repair run is asserted. The amendment still requires actual Linux elaboration of the exact integrated files before statement freeze and implementation. Original mathematical credit, formalization attribution, numerical-first obligations, dependency pins and original draft records are untouched.

`audit_amendment.py` actually completed successfully. It performed only bounded file checks and a read-only pinned Git blob read. No Lean, Lake, Comparator, cache operation, network request, author-source modification, shared development edit, Git mutation, publication or count change occurred. `MANIFEST.json` file paths are relative to this review directory.
