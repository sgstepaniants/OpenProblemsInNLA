# MI-04 independent pointwise orthogonality repair review

Reviewer: `/root/mi04_independent_referee`, independent of implementing author `/root`. **Approved as an exact source-only continuation for a fresh Linux run; no correction requested.** The candidate has complete 21-source closure `683be932b63873241336038e4eca8d53891dc42ab4bf0d344ade05a4fd3d8b5a`, author manifest `3ecc7be79def37f42acb5f6276e0ecac987b25f1012e877b3c02469cef985b02`, and new JointBasis source `ea3acff2f539beaeae34d665e1858a14b8b8acd2c195feab46d61df50ef0f584`. The proposed sources live only in the packet's `after/*.txt`; the active author sources still match the failed run.

I read the complete changed JointBasis module, exact patch, actual diagnostic window, packet metadata, and the relevant pinned primary definitions/lemmas in Subspace, Spectrum, JointEigenspace and Orthogonal. I also reread the frozen Definitions and the unchanged final Conclusion to check the repair in its actual mathematical role. This extends my prior complete MI-04 mathematical source review and subsequent repairs. It does not claim a fresh full reread of the twenty unchanged files.

The sole edit replaces the body of the private `jointEigenSpace_orthogonal` lemma. For distinct pairs of actual eigenvalues, the new proof introduces the two actual subspace vectors and states their two eigenspace membership facts explicitly. Distinct pairs have unequal first or second components. `Subtype.ext` shows that unequal actual-eigenvalue subtype elements have unequal underlying complex scalar values. In the first branch, both vectors belong to distinct eigenspaces of B; in the second, to distinct eigenspaces of A. The pinned `LinearMap.IsSymmetric.orthogonalFamily_eigenspaces` then returns the required ambient inner product equal to zero in the same argument order.

This matches the actual definition of `OrthogonalFamily`: a pairwise assertion quantified over two vectors, with the subtype isometries supplying their ambient representatives. Submodule membership in the intersection reduces to the two conjunction components supplied by `v.property` and `w.property`. The disjunction proof uses exactly the product-extensionality and negated-conjunction pattern present in pinned JointEigenspace. The applied one-operator theorem's scalar indices are inferred from the explicit underlying-scalar inequality, and its vector arguments are explicitly packaged with the corresponding eigenspace memberships. I found no substantive mathematics or API-shape defect in this pointwise proof.

Repeated eigenvalues remain inside their full eigenspaces; the proof needs inequality in only one of the two scalar components. If the first components agree, the second branch supplies orthogonality, and conversely. The case of equal pairs is excluded only by the defining pairwise condition. Zero vectors, zero operators, singular matrices, one-dimensional or zero-dimensional auxiliary spaces, and empty joint eigenspaces require no additional premise. The unchanged spanning helper still uses commutation only to establish the full joint-eigenspace supremum; the unchanged subordinate-basis construction retains all multiplicities. The main simultaneous eigenbasis and downstream normal diagonalization therefore keep their original mathematical scope.

The permanent target remains the full implication from the universal positive-block spectral-norm inequality to an affine Hermitian representation for every positive complex dimension, including scalar and singular X and alpha=0. The repair does not introduce a simple-spectrum, invertibility, real-only, positive-gap or dimension-cutoff hypothesis. All 168 declaration headers, 21 public contracts, ten frozen boundary files, imports, ambient section assumptions and trust/resource settings are unchanged. The private helper still has the default 200,000-heartbeat ceiling; the existing 800,000 ceiling is scoped only to the main lemma and is unchanged. Avoiding a dependent family transport is a source-level rationale, not an observed speedup or guarantee that elaboration will now finish.

I independently authenticated the author's complete before/after maps. All 21 before sources equal literal Git at `ce33d037e9960d7bbb096eb2bf90266cb07213c7`, the actual failed run `35076646177` receipt, all eleven post-command maps, the preceding approved source closure, and the unchanged active author files. All 21 after hashes match the proposed closure; only JointBasis differs. A whole-file boundary comparison proves that the identified private lemma body is the entire edit. The complete patch, unchanged headers, frozen files and four primary API files were rehashed; each API also matches its exact pinned Mathlib Git object and blob identity. My prior source review's 14 manifest bindings and prior actual-runtime review's 53 bindings were rehashed.

The observed failure is real: JointBasis line 42 exceeded the deterministic `whnf` heartbeat limit; line 81 then referred to the missing private constant, and the public kernel-trust assertion at line 98 rejected its self-axiom dependency. This is a failed module, not a successful verification. I reread that complete diagnostic window and retained it with the full log digest. The new pointwise body addresses the site of that failure, but no new Lean run was performed or inferred.

The scoped referee assessment covers correctness, full-target fidelity, edge cases, appropriate primary API use, resource discipline and attribution. George Stepaniants's Caltech department/university formalization credit, Colbrook's Cambridge DAMTP mathematical credit, Apache license and AI assistance remain unchanged. No email is introduced.

`audit_repair.py` actually passed using static Python and read-only Git. I did not run the author's preparation script, Lean, Lake, Comparator or a network fetch, and edited no proof/source/worktree/Git state. Fresh exact-source compilation, permitted-axiom/default-kernel checks, canonical Comparator and controls, and the exact publication/upstream runs remain pending. This approval changes no verified count.
