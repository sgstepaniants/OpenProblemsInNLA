# RA-02 review and execution gates

This plan is not an approval or execution receipt. The statement author is
`/root/mf22_publication_referee`; after authoring the draft, that agent's earlier
mathematical preflight cannot count as either independent statement review.
Two nonauthor agents must separately read the complete original target and
manuscript, numerical-first document, every concrete definition and all 27
Challenge contracts, then bind their findings to exact hashes. Root review,
actual Linux statement elaboration and an immutable statement freeze must
precede any proof implementation.

## Scoped Tau Ceti standards

Read the retained TauCetiReview REVIEWING.md and correctness, generality,
proof-quality, reuse and attribution rubrics at
`afb424eda89e8ac96d9eb69f6a88972055a4cd1b`. Apply them within RA-02's scope.
This is not official Tau Ceti approval, a Tau Ceti CLI run, or external human
peer review. Adversarial checks must reject weakened quantifiers, a restricted
sample-space expectation, assumed normalization/PSD preservation, a surrogate
spectral tail, integer-only p, unproved positivity, and structures whose fields
assume the desired conclusion. Inspect zero residuals/diagonals, all paths,
complex inputs, r=n, r=1, and the exact ordering of C,p,n,A,r quantifiers.

The concrete arrowhead-positive-definiteness obligation is a nonvacuity check.
Both state formulas must follow actual updates, and the expectation must
sum all paths. Count binary histories symbolically; do not infer an all-rank
statement from finite checks. Use existing ordered spectral, positivity,
Rayleigh, congruence and real-power asymptotic APIs. Stronger intermediate
Hermitian Rayleigh semantics are natural reusable foundations, not a hypothesis
added to the final target. Preserve original Colbrook mathematical attribution.

After implementation, two nonauthor final referees must each read every active
proof module, all exported types and the actual certificate dependency. They
must compare the source with the frozen contracts and inspect real runtime and
axiom evidence. A wrapper-only review is insufficient. Referee independence
must be disclosed anew if anyone authors or prescribes substantive repairs.

## Numerical work

One planned consumed LeanCert point certificate is Real.exp 1<=3, with explicit
kernel trust. All r,t,epsilon, matrices, eigenvalues, trace products, prefix
sets and real exponents stay symbolic. Avoid large rational expansion,
factorial path enumeration, numerical eigenvalues and interval subdivisions.
The eventual final-factor proof must depend on the certificate; an unused
standalone numerical example would not satisfy the requirement.

## Comparator and actual evidence

Read the primary Comparator README at
`2312244ac716564a61cc0bf4e107d9abf1757a61`. All 27 declarations are configured;
there are no definition holes. The only permitted axioms are propext,
Classical.choice and Quot.sound. Deliberate Challenge placeholders do not
count as proofs, and Solution must not import Challenge. At this draft stage
there is no Solution target, no Solution file and no proof-acceptance claim.

Acceptance requires real non-root Linux runs with the pinned dependency
manifest, full Solution build, kernel-mode LeanCert evidence, default-kernel
export/replay, exact Comparator matches, complete axiom reports, and required
negative/rejection plus Landrun sandbox controls. Compare all input hashes
before and after every relevant command with the actual checked-out Git commit;
read the full logs and retain authenticated artifact digests. Distinguish an
API head commit from a synthetic upstream merge checkout. A green badge or
successful Challenge elaboration alone is not proof acceptance. Independent
exact publication-commit reruns remain mandatory before counting.

No local Lean, Lake, cache, proof implementation, Git mutation, status change,
workflow dispatch or publication is authorized by this draft. Metadata remains
unchecked, whole_problem_verified=false and main_results empty. Actual
statement elaboration is pending. Any proposed semantic change must be reviewed
and separately retained before the final freeze. Sealed packets are immutable.
