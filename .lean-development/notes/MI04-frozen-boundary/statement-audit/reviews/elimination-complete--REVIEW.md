# MI-04 independent statement review

Verdict: **APPROVE** the exact 21-declaration mathematical boundary. No mathematical correction is requested. This is a source-only review, not proof acceptance, successful Lean elaboration, Comparator evidence or a verified-count increment.

Reviewer: OpenAI Codex agent `/root/next_elimination`. I have not implemented or edited any MI-04 definitions, statements or proofs. I independently read the complete canonical target, complete Colbrook proof (including its converse), exported source, numerical obligations, all definitions, all 21 Challenge declarations and the checking metadata. `INPUTS.json` binds all package bytes; `CHECKS.json` binds the three requested statement hashes and source/API checks. The original source matches immutable upstream `ce47b5630bf3680d9211131c3a43825b022c139a`, and the complete original proof is embedded unchanged in its exported TeX.

## Scope and actual mathematical objects

The final declaration explicitly quantifies every positive finite dimension and every complex matrix X, then every Hermitian pair A,B whose literal block completion is positive semidefinite. Its conclusion explicitly supplies a Hermitian K and arbitrary complex scalars alpha,beta with X=alpha K+beta I. Singular matrices, repeated values, scalar matrices and alpha=0 are retained. There is no selected-completion, invertibility, normality or spectral-genericity assumption in this final target. The source's stronger converse/equivalence is correctly outside the proposed formal claim: necessity is the full permanent question.

`spectralNorm` is the norm of the actual complex Euclidean continuous linear operator, with the index type explicit. It is not an entrywise or Frobenius norm. Blocks use the entire disjoint sum of two coordinate sets. `Matrix.PosSemidef` is the genuine predicate, including Hermitianity; the additional explicit A/B Hermitian assumptions match the question. `topValue` is the actual supremum of real Hermitian quadratic forms over the unit sphere. Six variational obligations establish attainment/maximality, PSD characterization, positive norm, shifts, scalar order and unitary invariance, so the desired spectral conclusions are not smuggled into definitions. Nonempty coordinate assumptions avoid an empty-sphere supremum in every generic variational theorem.

## All 21 contracts and proof route

The six generic variational contracts are mathematically valid and supply the needed norm/order bridges. `pencil_isHermitian` holds for arbitrary X and Hermitian T, including zero dimension in that auxiliary theorem. `universal_to_extreme_symmetry` retains all Hermitian T: the trace-zero pencil has nonnegative opposite extrema; its boundary positive-semidefinite shift yields one inequality, and the sign-flip unitary with T replaced by -T yields the other. The proposed shift may be singular, and the hypothesis includes it. `extreme_symmetry_scaled_unitary` is valid for every positive scaling and arbitrary unitary basis change.

I checked the two new perturbation contracts directly. Let g_j=1-d_j, w_j=V_ja/g_j away from a, L=sum|V_ja|^2/g_j and S=||w||^2. The test vector e_a+epsilon w has norm squared 1+epsilon^2 S. Its perturbed quadratic numerator is exactly 1+epsilon^2(S+L)+epsilon^3 Re(w*Vw). Subtracting one and dividing by epsilon squared gives the stated lower bound. For an arbitrary unit vector, completing weighted squares gives the upper bound with denominators g_j-epsilon||V||. The assumptions give g_j>=1/2 and epsilon||V||<1/4, so every such denominator is positive. No lower bound on d_j is needed for the generic sandwich. The one-sided squeeze produces precisely the claimed second coefficient, without analytic eigenvalue branches or a numerical root oracle.

`weighted_magnitude_identity` has the right row/column denominators and includes the equal diagonal terms. Both block pencils have a unique peak when the other diagonal entries lie in [-1/2,1/2]. The two allowed probes 0 and 1/2 change one weight from1 to2/3, establishing `offDiagonal_magnitude_symmetry`; arbitrary unitary basis extension gives `orthonormal_pair_symmetry`. Summation over a completed orthonormal basis proves the norm equality for X and its adjoint needed by `pair_symmetry_normal`. The complete normal unitary diagonalization remains an explicit obligation; the inspected pinned joint-eigenspace and subordinate-basis APIs are relevant foundations, not an asserted proof of that bridge.

`pair_symmetry_unitary` is the exact geometric invariance. For `diagonal_pair_collinearity`, the replacement vectors (1,1,1)/sqrt3 and (1,I,-1-I)/2 have squared norms1 and inner product0. The resulting equality is |a+Ib|=|a-Ib| up to the same nonzero normalization; its squared difference is 4 Im(a conjugate(b)). This preserves the source orientation conclusion and avoids roots of unity. Repeated indices or eigenvalues satisfy the same conclusion directly. `collinear_values_affine` includes the all-equal case and reconstructs real coordinates on a line otherwise. `pair_symmetry_essentially_hermitian` conjugates those real diagonal coordinates back to an actual Hermitian matrix. Dimension1 and dimension2 therefore need no artificial triple or genericity premise. The final explicit implication composes these full bridges.

## Scoped referee standards and remaining gates

This review follows the repository's adaptation of Tau Ceti: correctness and quantified scope, degenerate cases, proof quality/computational simplification, reusable primary Mathlib APIs, clear definitions and attribution. Exact Rayleigh bounds, two rational probes and elementary normalized vectors are appropriate reductions; no interval subdivision is needed. Matthew J. Colbrook keeps original mathematical credit and George Stepaniants receives formalization credit with the Caltech Computing and Mathematical Sciences department. The metadata includes no George email and distinguishes AI assistance from external human review or official certification.

The 21 Comparator theorem names exactly match the Challenge. There are no replaceable definition holes and only the ordinary standard axiom allowance. All 21 Challenge placeholders remain specification obligations, and no Solution implementation is present. Actual Linux elaboration, the second independent statement approval and an immutable freeze are still required before proof implementation. Later full proofs must discharge the normal diagonalization and every variational/algebraic bridge; their difficulty is not a reason to weaken this approved boundary. Final proof-source, Comparator, default-kernel, axiom and control evidence remain entirely pending. No local Lean, Lake, cache download, workflow dispatch, canonical edit, commit or push was performed.
