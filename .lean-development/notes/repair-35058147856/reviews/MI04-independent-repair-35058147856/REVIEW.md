# Independent MI-04 BasisTransport repair approval

**Verdict: approve the three exact proof-body repairs for the next compilation.** No mathematical target, declaration header, hypothesis, conclusion, definition, trust setting or dependency is changed. This approval extends my prior complete-source review and its separately sealed Nonempty and Probes addenda. It is not a claim that the repaired candidate has compiled, passed Comparator, or completed canonical verification.

Reviewer: `/root/mi04_independent_referee`, an independent nonimplementing AI agent. Root authored the patch. I read the complete current `BasisTransport.lean`, its exact three-hunk patch, the complete 469-line actual MI-04 implementation log, the frozen definitions and all 21 Challenge obligations, the separate operational referee report, and the relevant exact pinned Mathlib APIs. I did not edit author source, execute Lean or Lake, dispatch a workflow, mutate Git, publish, or change a count. The independent Python audit retained here executes only static file/Git/evidence checks and writes this review packet.

## Source and evidence boundary

The original repair packet manifest is `38c1c42a69357371aa0558da4c7c36828cc75fba7646720cb795a4b80aff6b59`; its checks are `567b7ae96a381f1932bc7914084072eb89670a4c7b912f6f11150c323b36ff58`. The exact patch has SHA-256 `7b9282667a56c7f66a4ca9dca1719e5291eb71d23e53f757b37b0a93653b6f7f`. `BasisTransport.lean` changes from `a3bd7f091903150bcf01c981a5a1ae8aae7bc8c31ea00f4b601aa53636c9341e` to `b0425ada4b3e2a58d0280929236f92e3a1017c2216f5d09dc8f52163834e8189`.

I independently matched all 21 before-source files to the actual failed Git commit and receipt, using the documented development transport `Solution.lean` → `NLA/MI04/Complete.lean`. I matched all 21 current files to the complete repair closure and retained after copies. Exactly one file changes; all twenty others are identical. All ten frozen inputs agree with the earlier complete review and both source closures. All seven post-command source maps equal the failed run's initial 1,633-input map. The complete previous review packets and their approvals remain sealed and were hash-checked; their chain is recorded in `PRIOR-FULL-REVIEW-CHAIN.json`.

The actual failed run is **35058147856** at **d36c8b357347a78781e854328a95fd5e4adce852**, job **104672556977**. Its MI-04 log hash is `1d562c1c0444f3e77af1a17c6703a21b1bd66fa3896acb20472e0561ed1eaeb2`, and its receipt hash is `f4fba693d4077a75212b742ab7d4a9010bb64bc55dbbdb4fb8604b0d104a73c6`. I reconciled the retained ZIP members and receipt with the authenticated API artifact digest and independently checked that all 407 nonempty MI-04 log lines occur in order in the retained raw job stream. I did not re-fetch GitHub in this review; the separately authenticated operational packet and actual API bytes are retained in the development-run directory. Its reused shared script's fixed `/root` auditor label is disclosed: the actual executing operational reviewer was `/root/ie13_continuation`.

The run genuinely failed. It reports `Probes` and the preceding fourteen MI-04 modules built, then only `BasisTransport` as a failed module. The downstream `orthonormal_pair_symmetry` axiom report includes `sorryAx` from failed elaboration and its LeanCert assertion rejects it. This is failure evidence, not an accepted proof or an intentional submitted axiom. The development run did not execute Comparator or strict default-kernel replay. None of those successes may be inferred from this repair review.

## The three repairs

1. **Use the existing norm-one hypotheses in the four-case simplification.** The two unresolved diagonal cases in the real log reduce to `‖y‖ = 1 ∨ ↑‖y‖ = -1`, with the needed `hu` or `hv` already present. The repaired simplifier explicitly receives both facts. The off-diagonal cases continue to use `huv`, its symmetric form and proved distinctness. Mathlib's `orthonormal_subtype_iff_ite` is exactly the Kronecker-delta inner-product characterization used here. No new norm, orthogonality or dimension assumption is introduced.

2. **Qualify `Module.Basis.toMatrix_apply`.** At the pinned Mathlib revision, the definition and theorem are under `namespace Module.Basis`, and the theorem is definitionally `e.toMatrix v i j = e.repr (v j) i`. The previous unqualified `Basis.toMatrix_apply` was genuinely unavailable in this namespace, as the actual diagnostic reports. Combining the qualified theorem with `OrthonormalBasis.coe_toBasis_repr_apply` and `EuclideanSpace.basisFun_repr` produces precisely `(basisUnitary B) i j = B j i`. The order is column `j`, coordinate `i`; no transpose or adjoint is silently exchanged.

3. **Instantiate the reversed coordinate-coefficient identity.** The old rewrite attempted to replace the metavariable pattern `?X ?i ?j`. The new call supplies the concrete matrix `unitarySimilarity X (basisUnitary B)` and both indices before reversing the same proven equality. It then uses the unchanged unitary-similarity coefficient identity and the two basis-action identities. This establishes the same `Uᴴ X U` entry as `⟪B i, X(B j)⟫` with the original complex inner-product convention. No equality is assumed or weakened.

The full file still extends an arbitrary orthonormal set to an actual orthonormal basis, reindexes that basis to `Fin n` using its genuine dimension, constructs the actual change-of-basis unitary, and transfers coordinate magnitude symmetry to arbitrary orthogonal unit vectors. The pinned extension, reindexing, coordinate-representation and unitary-membership APIs were inspected and byte-compared with the local exact Mathlib Git object at `0df444a360eaa60ab8c11dca51a86af692955474`. `PRIMARY-API-BINDINGS.json` records all three primary files and inspected line intervals. The patch is exactly the three listed replacements; all nine declaration headers in this file are unchanged.

## Full target, provenance correction and remaining gate

The universal complex matrix target remains unchanged: in every positive dimension, the original norm inequality for every Hermitian positive-semidefinite block completion implies an affine-Hermitian representation `X = α • K + β • I`. It uses the actual complex Euclidean operator norm. Singular, scalar and zero matrices, repeated eigenvalues and `α = 0` are retained. The repaired helper treats arbitrary orthogonal unit vectors rather than only coordinate vectors and introduces no dimension-two exception. All 21 public contracts, frozen definitions, numerical plan, provenance, dependency pins and prior mathematical route remain intact. The repair adds no numerical interval computation and preserves all LeanCert trust assertions.

I found stale provenance wording inherited from an older Probes-only correction in the initially supplied closure `0b3840f310be1d68f957d10862e721f960dcf0f0bd0420c8a38c1f0ca9c82099` and reported it. Root appended a metadata-only clarification without altering that original sealed packet. I independently checked that exactly three metadata fields changed and all 21 source hashes and ten frozen hashes stayed identical. The clarification properly distinguishes the immediately superseded metadata record from the proof-source predecessor `3f553e705896b765090592e3a637fb202cf165bea9dbc01f437d6507298ec3c7`.

**This approval binds the current clarified closure `0739d4df0299ae9be0f03aa20c71e9c96f8875a8384bc39897dc497677e9d81c`.** Its clarification manifest is `226086bc10535220a471899c1fc1b08960d3d7d31b6758e8136c2feae191926c`; checks are `17a134c5d2aa973a0fec50f92696df58fcfcbbd9f01ac43ed8db51824fd928c9`. The original proof repair packet remains the source of the exact patch.

No further source correction is requested by this review. The source is approved for a fresh development build; successful complete compilation, strict canonical LeanCert/default-kernel/Comparator checks and authenticated exact-commit evidence remain required before acceptance or any count change. Attribution continues to distinguish Colbrook's original mathematical argument from George Stepaniants' Caltech Department of Computing and Mathematical Sciences formalization and disclosed AI assistance. This is scoped independent AI review, not formal verification by itself or external human peer review.
