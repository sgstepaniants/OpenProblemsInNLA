# Independent IE-13 observed-error repair approval

**Verdict: approve the seven proof-body repairs for a fresh compilation.** I found no mathematical correction needed in this patch. All 27 active sources are reconciled to the previously approved complete candidate and actual failed Git inputs. Exactly three files change; all 238 declaration headers, 28 public contracts, ten frozen inputs, imports and declaration scope are preserved. The repaired candidate has not yet established compilation or canonical checker acceptance.

Reviewer: `/root/mi04_independent_referee`, an independent nonimplementing AI agent. `/root/ie13_continuation` authored this repair. I read the complete current `WitnessEntries`, `WitnessForward` and `LateColumn` modules (130, 155 and 92 lines), the exact patch, all 361 lines of the actual failed IE-13 implementation log, the relevant pinned APIs and frozen target boundary. This is an addendum to my complete 27-source mathematical review and the previously approved five-file repair. I executed only my own static Python/Git audit, not the author's repair auditor, Lean, Lake or any proof consumer. I edited no author source, Git state, PR or count.

## Exact packet and prior review chain

The author packet is `elimination/IE-13/proof-handoffs/repair-35058147856`. Its manifest SHA-256 is `163dfd93d2cc712f3059fcad6fd2c3a4c574370ca58698d69c70682c765d0b4f`; checks are `c27ab112f369cd6ae351e886a8cc74ed8dedce0ccea669bdf3242178254f7c13`; the exact patch is `a30559d66cbc60a5ffda2c8e667db9ecadff91e5a14bb46efab1a939376433b9`. I verified the sealed packet and before snapshot hashes and independently matched every before source plus Challenge to its actual Git blob and failed-run receipt. All current held sources match the after snapshots. The other twenty-four sources are byte-identical.

Every before source equals the after source approved in my prior repair packet, manifest `0fd4f5c76d228b250c37ccaa987c26a43e7758e6fca7ea0ca575e4c01ecc9533`. That approval extends the full-source review sealed by `4ac7e8cdae683c780b51cdcafa3cc6869efba4ccc1d4c41fe4ef544c0c6480c4`. Both earlier packets were independently hash-checked again. The present checks compare all 238 declaration headers, ensure exactly the seven named proof bodies changed, and compare all 28 public headers with the unchanged frozen Challenge. No ambient section or import change can introduce an extra premise through this patch. All ten frozen files remain exact; the statement-freeze identity is `85916aae89eb2111ff914b8767e8918548c132a6ff6c7ed92e52286bec1998fe`.

## Actual failure evidence

The run is **35058147856**, actual commit **d36c8b357347a78781e854328a95fd5e4adce852**, job **104672556977**. Its whole-development receipt hash is `f4fba693d4077a75212b742ab7d4a9010bb64bc55dbbdb4fb8604b0d104a73c6`. The receipt's seven post-command maps all equal its original 1,633-input map. The retained operational audit authenticates the complete run; the reused shared audit's fixed `/root` label is expressly disclosed as execution by `/root/ie13_continuation`.

I independently checked the actual IE-13 log against the artifact ZIP and receipt and matched all 300 nonempty log lines in order to the raw GitHub job. The actual run failed in precisely `WitnessEntries`, `WitnessForward` and `LateColumn`. The earlier five repaired modules now report successful builds. The old `witness_structure` and `forward_lower_action` reports include `sorryAx` from failed elaboration and fail their LeanCert checks. The standard-axiom report for `late_column_zero` is inside a failed module and is not evidence of a successful build. This was a development compilation run; it did not run Comparator or strict default-kernel replay. These facts remain distinct from approval of the new source.

## Independent assessment of the changes

**WitnessEntries: optional constructor simplification, mandatory arithmetic proof.** The observed line-44 failure is exactly “`simp` made no progress.” After expanding the factor-row index and splitting its finite cases, some constructor projections have already reduced. Making only that simplification optional is appropriate. The following unconditional `all_goals omega` still has to discharge every remaining arithmetic goal. No unresolved branch is admitted, and the proof still derives the original upper-support condition from `i.val < j.val` and the same factor-row permutation. The downstream band-support, unit input maximum and rational witness statements are unchanged.

**WitnessForward: eliminate actual `if True` terms.** The actual two diagnostics explicitly display residual `if True` expressions. Pinned Lean's `ite_true` states `(if True then a else b) = a` by reflexivity. Replacing unused `if_pos rfl` in these two simplifier lists uses exactly that API. It preserves the forward-window convolution, sequence recurrence, zero-index case and lower-factor action; no arithmetic statement is replaced by a weaker one.

**WitnessForward: explicit target-row cases.** I checked the literal `witnessFactorIndex` definition against the new proof. If `i.val < p`, the factor index is `i.val + 1`, which is positive and at most `p`; the target-factor value and the claimed original-column value are both zero. If `i.val = p`, the factor index is zero and both values are one. Otherwise `p < i.val`, so the factor index stays `i.val`, is positive and exceeds `p`; both values are one. The supplied inequalities are precisely those required by `dif_pos`, `dif_neg`, `if_pos` and `if_neg`. This exhausts all natural cases, including `p = 0`; it does not introduce `p > 0` into this general helper. The old broad conditional split produced eleven incompatible residual branches. The new proof resolves the same identity directly, with no change to either side or to the finite-index definition.

**LateColumn: three local classical instance scopes.** All four actual errors ask for `DecidablePred` on the existing filtered sets of active original rows. The pinned `Finset.filter` and `mem_filter` APIs require that instance, and pinned `Classical.propDecidable` supplies it. A `classical` command is added inside each of the three affected existing proofs, with no global instance or definition change. The proof still uses the original input's upper bandwidth, the actual moving-front label, and the genuine physical-row update. The induction on elimination steps and its zero-entry argument are unchanged; arbitrary permitted pivot choices remain quantified. Choice is already in the permitted standard axiom set, and the trust assertions remain in place.

The six relevant primary files were inspected at their recorded line intervals. Their SHA-256 and Git blob identities were recomputed. The Mathlib filter source matches the exact local Git object at `0df444a360eaa60ab8c11dca51a86af692955474`. The retained Lean core sources match the installed Lean 4.33.1 source bytes and identify the actual run's Lean revision `819816b2e0a3bf405af45ae5c7af2491d8f5bee6`. `PRIMARY-API-BINDINGS.json` distinguishes these source comparisons and does not claim a new compilation of the tools themselves.

## Preserved complete target and remaining gates

The final target remains the actual greatest growth value and supremum for the canonical unequal-bandwidth problem, with arbitrary permitted dimensions, full complex nonsingular inputs and every admissible GEPP pivot path. The actual finite entry/stage maxima, literal row swaps, Schur updates and normalization are unchanged. The universal upper bound, exact rational-real attaining witness, witness nonsingularity, admissible-prefix extension, target-column value and final greatest-value conclusion remain the same proof obligations. The separate lower-bandwidth-zero identity construction and the upper-bandwidth-zero edge remain included. No finite parameter cap, restricted tie rule or witness factorization assumption is introduced.

These are exact algebraic and finite-index repairs. They add no interval grid or numerical search and retain the existing LeanCert assertions and frozen Comparator configuration. George Stepaniants' Caltech Department of Computing and Mathematical Sciences formalization credit, Colbrook's mathematical credit, disclosed AI assistance and Apache-2.0 remain unchanged. No email was added.

The independent source review is complete, with no additional source edit requested. A fresh complete development build must still succeed, followed by strict canonical LeanCert/default-kernel/Comparator execution and exact-commit evidence authentication. This approval alone is neither a verified solution count nor a publication approval for unrun proof code. It is scoped independent AI review, not external human peer review or formal verification by itself.
