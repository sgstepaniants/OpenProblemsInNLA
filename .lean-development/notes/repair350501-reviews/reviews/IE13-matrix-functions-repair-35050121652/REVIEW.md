# IE-13: independent source addendum after run 35050121652

**Verdict: APPROVE the two proof-only repairs.** This extends my prior generic-prefix and Origin/Front mathematical source reviews to the exact repaired Maxima and Origin bytes. It does not certify a successful rerun, the two new Recurrence/Envelope modules, or the complete 28-target problem.

I read both complete repaired files, both exact before/after patches, the relevant frozen definitions and contracts, the full authentic IE-13 diagnostic log, and the pinned finite-set API. I independently matched all six original run sources to Git `b428c974e7d82b37a8c11ad7e8cb5dc4ebfb3380` and the actual command's pre/post receipt. The eight held after-files and all ten frozen inputs match the sealed packet and author tree. The unchanged earlier reviews, full evidence and exact source snapshots are retained with hashes in `CHECKS.json` and `INPUTS.json`; archived Lean sources use `.lean.txt`.

In Maxima, the failed `gepp_model_semantics` body left the structural `if h : k.val < n` unreduced. The repair first rewrites `trajectory` and applies the existing witness `k.isLt`, then simplifies the same Schur update, row swap and swap-at-pivot equality. A finite index with its existing proof represents the identical pivot. Stage zero, the actual physical swaps, the active-block conditional, the complex division and all tie paths are unchanged. This is a reduction-order repair, with no new assumption or changed algorithm.

In Origin, the failed `oldRows_succ_eq` body asked arithmetic automation to rearrange opaque activity and pivot-disequality propositions along with the numeric threshold. The repair explicitly carries those two propositions through both directions of the equivalence. Only `i.val < (k.val + 1) + p` versus `i.val ≤ k.val + p` is left to `omega`. Pinned `Finset.mem_erase` states exactly the required conjunction of pivot disequality and membership. The same active-original-row set therefore loses exactly the actual pivot label. This reasoning covers every natural bandwidth, including `p = 0`, and every admissible complex path; it introduces no special front-size invariant.

Every helper/public declaration header is unchanged. The remaining four previously submitted sources are byte-identical, and the full frozen problem boundary is unchanged. I request no further mathematical or concrete API correction to these two edits.

The actual run accepted Basic, GEPP and Prefix. It rejected Maxima and Origin at the two recorded proof expressions, and the dependent Front module was not accepted. Individual permitted-axiom prints inside a failed module do not promote that module. The repaired candidate still requires actual Linux compilation, and complete-problem acceptance still requires the remaining proofs, Comparator, default-kernel replay, controls and final independent reconciliation. No Lean/Lake execution or author-source edit was performed by this reviewer.
