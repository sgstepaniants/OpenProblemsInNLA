# MF-22 independent exact real-power repair addendum

**APPROVE the single proof-body repair at source level.** Reviewer: OpenAI Codex agent `/root/next_inequalities`, a non-implementing MF-22 mathematical referee. This extends my complete source review and retained repair addenda; it does not certify the repaired runtime or canonical checks.

I reread the complete Conditioning module and the exact before/after diff. The one change adds `Real.rpow_eq_pow` to the final restricted simplification. The frozen existential conclusion explicitly uses `Real.rpow (n : ℝ) α`, whereas the established quadratic estimate uses the natural power `(n : ℝ)^2`. The pinned primary theorem `Real.rpow_eq_pow` is the literal `rfl` bridge from the named real-power function to real-exponent power notation; the existing `Real.rpow_two` then gives the natural square. Neither lemma adds a side condition. The witness exponent stays 2, and the earlier positive constant, common threshold and actual determinant/inverse conclusions are unchanged.

This is the exact observed defect in failed Linux run **35050121652** at `b428c974e7d82b37a8c11ad7e8cb5dc4ebfb3380`, Conditioning line 77. I bound the before-file to Git and the authentic receipt, inspected the full retained diagnostic including the unequal natural-power/named-real-power types, and checked the primary API bytes at Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`. The parent operational audit `05034a994353382679797867cc35d8fc57aad987612c1311d9fa8ab3fc01780a` authenticates 976 input files; this report does not claim to repeat that entire audit.

The actual run accepted all prerequisites and printed permitted axioms for 21 frozen exports, including the real inverse-entry bound and quadratic conditioning theorem. The final polynomial theorem failed and LeanCert correctly rejected its resulting `sorryAx`; the complete entrypoint was not accepted. These are component results, not complete verification.

I reconciled every current source hash against my last 29-file approval: only Conditioning differs. All declaration headers, the ten frozen boundary files and 22 independent contracts are unchanged. The full mathematical approval therefore retains every positive parameter, the exceptional `rho^2 = 10` case, actual eventual nonsingularity and inverse, genuine Euclidean operator norms, and the original existential polynomial condition-number target. It still claims the sufficient exponent 2, not the stronger exponent 1 estimate in the manuscript.

No new source correction is requested. `CHECKS.json` and `inputs` bind the entire current graph, exact repair packet, API source, authentic log/receipt/root audit and prior reports. I made no proof edit and ran no local Lean/Lake. Successful repaired full development execution, source-matched canonical Comparator/default-kernel/axiom checks and rejection/sandbox controls remain required before final acceptance or any count change.
