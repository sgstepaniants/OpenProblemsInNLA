# IE-13 independent recurrence and envelope source review

**APPROVE this exact two-module source packet, with no mathematical correction or concrete pinned-API defect identified.** Reviewer: OpenAI Codex agent `/root/next_inequalities`, a nonimplementing IE-13 referee. Packet `d442b3b05dd7493b39ca94b0fb5abf173e516608b4392b57b9a95668b77336bc` implements four frozen exports and additional partial-sum helpers. These two modules are held for a future batch; the currently running check of earlier modules is not evidence that they have executed.

I read the complete Recurrence and Envelope modules, their frozen Definitions, all four exact Challenge contracts, and the associated source/numerical recurrence and sorted-envelope paragraphs. This supplements my separate original-row/front and generic-prefix reviews. The overall canonical question remains the sharp GEPP growth classification for every unequal natural bandwidth pair, all permitted complex matrices/dimensions and all legal pivot ties. This chunk proves only scalar identities required for that classification; it does not assert that actual matrix rows already obey its envelope.

The history-register induction correctly identifies every slot with `h_p(t-r)` under truncated natural subtraction. At zero time all slots and `h_p(0)` are zero. At positive time the zero slot is the definition of the sequence, while a positive slot shifts from its predecessor. The equality `(t+1)-(r+1)=t-r` is valid including `r>t`; no positivity of a truncated argument is assumed. Thus all nonpositive integer indices in the source are represented by the same genuine zero value. The exported recurrence is exactly `h_p(t+1)=1+sum_{r<p} h_p(t-r)`.

Monotonicity uses strong induction, not an assumed property of the recursively defined function. When `r<=s`, the next summand advances the smaller time `s-r`; otherwise the previous summand is exactly zero. Summing these termwise inequalities proves the next step. Every positive-time value is one plus a natural sum. Both conclusions include p=0, where the sequence is zero at zero and one thereafter, and no bound is imposed on p, time or history slot.

The envelope definition has a separate zero state. Its first state is all ones because each truncated sequence argument is zero. At positive time its first coordinate is the full sequence recurrence; every coordinate is at least one. Increasing the coordinate index shortens a finite range of nonnegative natural summands, so the envelope is antitone without assuming a stronger property. The step proof shifts the finite range to separate `h_p(t)` and treats the last coordinate separately, where the remaining contribution is one. This is precisely the source update `(x1+x2,...,x1+xp,x1+1)`, including p=1 with no middle coordinate. Positive p is required only by the exported envelope theorem that actually uses its first coordinate.

I also read every partial-sum helper. The successor identity splits the indicator `i<r+1` into the disjoint cases `i<r` and `i=r`, retaining the genuine finite sum over `Fin p`. Its monotonicity is termwise and works beyond p as well. For `0<=r<=p` and positive time, the final subtraction-free identity is

`E(t+1,r) + h(t) = E(t,r) + (r<p ? e(t,r) : 1) + r*h(t)`.

For r=0 it uses the first-coordinate identity. The successor induction combines the preceding partial-sum identity with the single-coordinate step and expands `(r+1)*h(t)`. This formulation correctly handles the full-sum endpoint r=p and avoids an unjustified natural subtraction of `h(t)`. It is the same sum of the source coordinate updates and supplies a valid identity for later subset bounds; it does not by itself prove those matrix bounds.

All four written export headers exactly match their frozen Challenge counterparts. The ten frozen files and original snapshots remain byte-identical. I checked pinned primary signatures for the finite-`Fin`/range conversion, the shifted range-sum formula, monotonicity from successor inequalities, and nonnegative finite sums under subset inclusion. Their orientations and nonnegativity premises match the uses here. The natural-subtraction branches were checked symbolically throughout. No specific missing namespace, reversed finite-sum direction or hidden initial-window assumption was found. This is not a guarantee that every tactic will elaborate before actual Linux execution.

The exact three-file dependency closure, frozen boundary, current source packet, prior incremental reports and pinned primary API bytes are retained under `inputs` and bound by CHECKS/MANIFEST. Every advertised export and the partial-sum step include LeanCert kernel trust assertions and axiom prints; no proof hole, custom axiom, native/unsafe shortcut or Challenge import appears in these implementation files. Original Colbrook/Cambridge mathematical credit and George Stepaniants/Caltech Computing and Mathematical Sciences formalization credit remain distinct, with no George email.

I made no author-source changes and ran no local Lean/Lake. No interval splitting, finite-parameter enumeration or precomputed sequence table is part of these universal arguments. Actual proof compilation, full remaining matrix/witness/extremum obligations, complete independent final review and canonical Comparator/default-kernel, axiom and rejection/sandbox gates are still required. This report does not count IE-13 as verified.
