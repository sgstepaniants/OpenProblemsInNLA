# IE-13 independent complete upper-bound source review

**APPROVE this incremental mathematical source chunk.** Reviewer: OpenAI Codex agent `/root/next_inequalities`, a non-implementing IE-13 referee. I read all five complete modules LateColumn, SubsetAlgebra, FrontBounds, ColumnBounds and UpperBound, their unchanged frozen contracts and definitions, and the canonical upper-bound argument. This extends my separate generic-prefix, original-row/front, and recurrence/envelope reviews. It does not establish attainment or certify the full problem.

The original target at immutable revision `ce47b5630bf3680d9211131c3a43825b022c139a` uses arbitrary nonsingular complex banded matrices in their original ordering, all allowed maximal-modulus pivot ties, and the maximum over every active Schur entry. The five exact frozen exports preserve that scope. The universal upper result is even stated for every positive dimension and every pair of bandwidths. Neither a selected pivot nor a final triangular entry replaces the actual active maximum. No front bound is assumed as part of the input or witness.

LateColumn derives the exact zero phase from the original upper band and physical original-label updates. The only potentially fresh label is `k+p`; if the target column has not arrived, its unchanged input entry also vanishes. Both the old survivor and actual pivot therefore have zero target values, so elimination preserves the zeros. This reasoning includes a missing fresh row near the bottom.

SubsetAlgebra proves the needed all-cardinality statement without sorting or enumeration. A subset of the enlarged front contains at most one fresh label; erasing that label returns an old subset. Old envelope coordinates are at least one at positive age, giving both the small-subset estimate and the full-front estimate. For a surviving subset of size r, the triangle inequality gives its updated sum at most the old sum over its union with the pivot plus `(r-1)` times the pivot magnitude. The singleton and union estimates combine with the exact envelope partial-sum recurrence. The empty subset, full r=p endpoint and p=1 case are explicitly retained, and every multiplier is the genuine complex quotient bounded by pivot admissibility.

FrontBounds transports this abstract inequality to the actual current front and deletion of the actual pivot. Its first nonzero arrival is treated separately: all old values vanish and at most one fresh target value can be nonzero. If the surviving row is old, its new magnitude is at most the pivot bound; if it is the fresh row, the pivot must be old and its target value is zero. Thus the first envelope consists of ones, rather than incorrectly applying a positive-age recurrence at age zero.

ColumnBounds inducts over the actual physical trajectory. Early columns start with the all-ones envelope at stage zero. Late columns retain their zero phase, then make the zero-to-one update, then increment their exact age at each step. When a column is still active its age is at most p+q. UpperBound applies the singleton subset estimate to every old active original row; every remaining active row is a future unchanged input row. The true input maximum is kept as a multiplicative factor until the final division, whose positivity follows from nonsingularity. For p=0, the old front has cardinality zero, all active entries remain input entries, and the actual growth equals one. This covers q=0 as well.

Two kinds of repairs are bound separately. The **observed** Maxima and Origin changes respond to failed run35050121652: Maxima explicitly rewrites the dependent trajectory conditional before simplifying the literal Schur expression; Origin explicitly carries the activity and pivot-disequality propositions through the survivor-set equivalence and uses omega only for natural thresholds. I reread both complete repaired modules, their actual diagnostics, exact Git-before and primary Finset membership API. They preserve all prior mathematical source conclusions and headers. In that run Basic, GEPP and Prefix compiled, while Maxima and Origin failed and their trust assertions correctly rejected affected `sorryAx` dependencies; Front was blocked.

The **predictive**, unexecuted SubsetAlgebra change is exactly the independent root finding: the pinned unrestricted `add_le_add_left` appends its common summand on the right. Replacing it by `add_le_add (le_refl ‖v i‖) this` proves the needed left-fixed norm inequality directly. I verified the primary `to_additive` definitions, original packet, exact one-line diff and current closure. This is not represented as an observed error or accepted runtime result for the five new modules.

I found no additional mathematical or concrete API correction. All five written theorem headers match Challenge; all ten frozen files are unchanged. Original mathematics remains credited to Matthew J. Colbrook, Cambridge DAMTP; formalization remains credited to George Stepaniants, Caltech Department of Computing and Mathematical Sciences, without a contact email. All fourteen current dependency sources, original and predictive packets, observed repair packet, primary API sources, original mathematical sources and prior reports are retained under `inputs` with exact hashes.

The rational attaining witness, complete legal attaining path, exact growth equality and greatest/supremum final contract remain required. No new module compilation, repaired-foundation acceptance, full LeanCert/Comparator/default-kernel acceptance or solved-problem count is inferred here. I made no author-source edit and ran no local Lean/Lake.
