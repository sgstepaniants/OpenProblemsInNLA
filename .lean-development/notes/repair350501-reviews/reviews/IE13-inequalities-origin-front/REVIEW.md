# IE-13 independent original-row and front source review

**APPROVE this exact two-module chunk at source level, with no mathematical correction or concrete pinned-API defect identified.** Reviewer: OpenAI Codex agent `/root/next_inequalities`, a nonimplementing IE-13 referee. The reviewed packet is `a42c18f33d89bc9118c1c33e873554721c78f4349594d9118c246011879cdd24`. This is an incremental review of four contracts, not acceptance of the whole 28-contract problem or a claim that these proof modules have compiled.

I read both complete Origin and Front modules, their complete Basic/Definitions dependency sources, all frozen Challenge contracts, the numerical dossier and source correspondence, and the original canonical target and manuscript. The canonical target concerns all unequal natural bandwidth pairs, every permitted finite dimension and nonsingular complex matrix, every legal maximal-modulus pivot tie, and every active Schur entry in the given input ordering. The frozen literal trajectory starts from that original input; its row choices are current physical rows and its update is the actual complex Schur quotient. The origin map composes precisely those physical swaps, rather than introducing a preliminary permutation. I then read the two retained statement reports as supplementary context.

`originalRow_update` correctly transports the survivor identity through the inverse origin permutation. Applying the next swap twice returns the previous physical row. An active original label distinct from the actual pivot label lands strictly below the new pivot, including a survivor originally at the pivot position. The exact active-set equivalence removes precisely the selected original label. The target column is strictly trailing, so the Schur branch used in the formula is the correct one. I checked both swap orientations, their inverse, the no-swap case, and the fact that old inactive rows cannot become active. `multiplier_bounds` exposes the actual nonzero pivot and obtains the complex quotient bound directly from the genuine all-active-row maximal-modulus premise. No real-only argument or restricted tie choice occurs.

`front_transition` is the exact finite-set deletion identity. At the next stage, label `i < k+1+p` is equivalent to `i <= k+p`; activity is previous activity minus the actual pivot label. This identity holds for any position-valid step; the band assumptions in its public header are harmless unused hypotheses, not missing restrictions. All three public Origin headers are byte-identical to their independent frozen Challenge headers.

The general-p front proof derives its invariant rather than assuming it. Initially the old labels inject into `range p`. At any stage the front has at most the old cardinality plus one: if label `k+p` exists it is the sole possible fresh row, and otherwise the front is a subset of the old set. Given untouched future rows, a pivot with original label greater than `k+p` would still equal its original zero band entry in column k, contradicting the required nonzero pivot. The chosen pivot therefore belongs to the front. Deleting it and using the cardinality bound keeps at most p old survivors.

Future-row preservation is simultaneous with that cardinality induction, not circular. A label at least `k+1+p` was previously untouched, lies beyond the possible pivot label, and has original column-k entry zero by the lower band condition. Its Schur multiplier is therefore zero, so each remaining active entry retains its original value. This proves future preservation for the next stage and closes the induction through stage n. The final `front_structure` has its exact frozen header and no extra invariant hypothesis. The argument covers arbitrary p and q, p=0, q=0, p larger than the dimension, no-swap and nontrivial-swap steps, missing fresh rows near the bottom, and the final pivot. Finite-set truncation handles absent rows without adding fictitious entries. Upper bandwidth q is intentionally unnecessary for this lower-front lemma and remains available for later target-column bounds.

The reused generic origin/survivor source matches immutable IE-14 revision `6e48f25fffdae2cf93e4985dc515abbd15e0481b`; no cyclic/two-row front conclusion is imported. I checked the pinned primary Equiv composition/inverse/swap identities, the orientation of `lt_of_le_of_ne`, and the exact signatures of `Finset.card_le_card_of_injOn`, `card_insert_le`, and `card_erase_add_one`. Their arguments and orientations match these proofs. Fin order coercions are the genuine underlying natural comparisons. The checked helper calls therefore reveal no specific missing namespace or reversed inequality API. Actual elaboration can still uncover issues that a source review does not establish.

`CHECKS.json` and the retained inputs bind the four-source dependency closure, exact four headers, all ten unchanged frozen files and their snapshots, canonical Git inputs, the original IE-14 file and pinned primary API bytes. Each advertised export has a LeanCert kernel trust assertion and axiom print; no proof hole, custom axiom, unsafe/native shortcut or Challenge import is present in this chunk. These source checks do not substitute for actual trust execution. The historical statement-only wording in frozen documents is preserved and is not a current execution claim. Colbrook's Cambridge mathematical attribution and George Stepaniants's Caltech Computing and Mathematical Sciences formalization credit remain distinct, with no George email.

No author files were edited and no local Lean/Lake command was run. The four reviewed contracts do not by themselves establish the envelope recurrence, full all-active-entry upper bound, rational witness, path extension, or final sharp supremum. Those obligations, full-project independent reviews, actual Linux compilation, Comparator/default-kernel and axiom checks, and rejection/sandbox controls remain separate gates.
