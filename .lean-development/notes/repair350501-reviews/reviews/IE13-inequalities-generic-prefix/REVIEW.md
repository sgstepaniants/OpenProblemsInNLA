# IE-13 independent generic prefix source review

**APPROVE the exact Basic/Maxima/GEPP/Prefix chunk at source level.** Reviewer: OpenAI Codex agent `/root/next_inequalities`, a nonimplementing IE-13 referee. No mathematical correction or concrete pinned-API defect was identified. This supplements my separate Origin/Front review; it does not accept the complete original problem, claim actual compilation of this chunk, or pre-approve later source changes.

I read all four complete modules, their frozen Definitions, the six independent Challenge contracts and their surrounding full boundary, and reconciled the original target and manuscript already read for the front review. The canonical problem still concerns every unequal natural bandwidth pair, every allowed nonsingular complex input dimension, every legal maximal-modulus pivot tie, and the maximum over all active entries in the given input order. These generic helpers neither constrain bandwidths nor replace this target with a particular pivot rule.

The maxima are genuine finite maxima of complex moduli. Basic proves the finite-sup bounds, attainment on positive dimensions, stage-zero identity, and bounds for the actual numerator. Maxima restricts its active-attainment search to rows and columns at least k; that finite set contains `(k,k)`, even at the final scalar. Inactive zero padding therefore cannot falsely supply an allegedly active attaining entry. The stage maximum is attained as well. If the input maximum were zero, every actual entry would vanish, contradicting nonzero determinant in the explicitly positive dimension. The denominator is consequently positive before every division/cancellation that needs it. Growth includes the original input and hence is at least one; the equality `growth * entryMax = peakMax` yields both every active-entry bound and an actual attaining stage/entry. These bookkeeping statements legitimately work even for an arbitrary supplied path; later legal-path results still require admissibility.

`gepp_model_semantics` unfolds the actual physical swap and Schur subtraction and starts with the original input. It does not assert a bound or assume nonsingularity. The generic `ActiveInjective` predicate is exactly zero-kernel behavior of the active submatrix: the vector is zero before k and the matrix product vanishes in every row from k onward. Padding outside the active block does not change that mathematical meaning. Initial injectivity follows from the actual determinant via the matrix-unit and matrix-vector injectivity theorems. An active zero column would put the nonzero coordinate vector in this kernel. Physical row swapping preserves the active equations because both swapped indices are active.

The Schur injectivity proof uses the standard explicit lift. For a trailing kernel vector x, it adds the pivot-coordinate correction `c = -(B*x)_k / B_kk`. The pivot denominator is proved nonzero from the original chosen pivot after the swap. The corrected vector y has zero active product in the pivot row and all trailing rows by the exact Schur formula. Previous active injectivity forces y to vanish; x already vanishes at the pivot and equals y at every other coordinate. Thus x is zero. This proof works over complex scalars, for nontrivial or identity swaps, and through the empty trailing block after the final pivot. It assumes no unproved determinant identity for a padded singular whole-stage matrix.

Maximum-pivot choice searches the nonempty actual active column. Its modulus dominates every candidate, and the proved nonzero active entry forces that maximum itself to be nonzero. The recursive maximum trajectory is then genuinely admissible at every step by induction. The chosen path is one existence witness; the public admissibility predicate remains the complete set of maximum-modulus ties. No uniqueness or deterministic tie condition is introduced.

Prefix is a separate literal completion construction. Before t its recursion uses exactly the supplied current-row choices; afterward it uses the actual maximum of the state it has itself produced. The trajectory equality is proved by structural induction, and agreement of choices before s implies agreement of states at s. This establishes every state through t, not merely the last prefix state. During the injectivity induction, steps before t use the supplied admissibility only after transporting by that independently proved prefix-state equality; later steps use the new state's genuine maximum pivot. There is no assumed admissibility of the unused original suffix and no circular assumption that the completed trajectory is already valid.

The resulting extension covers every public `t <= n`, including t=0 and t=n. At zero it simply constructs an admissible maximum-pivot trajectory; at n it retains all original choices and all states through the final update. Some private lemmas prove stronger statements without needing the public dimension/prefix-length bound, which is harmless. Positive dimension, nonsingularity, the full allowed prefix, all legal ties and the exact endpoint state are retained in the frozen public contract.

I independently compared all six written theorem headers with Challenge: they are exact. All ten frozen files and their snapshots are unchanged. The complete Basic proof body is the recorded IE-14 generic source with only its documented helper rename; the complete reused active-injectivity/maximum-path body is byte-identical to that immutable IE-14 source. The new Maxima and Prefix bodies were read independently rather than accepted by reuse. I checked the pinned primary finite-maximum, finite-sup, determinant/unit, matrix-vector injectivity and coordinate-vector API signatures and orientations. No specific namespace, order-orientation or missing-hypothesis defect was found; actual elaboration remains required.

Every public export has a LeanCert kernel trust assertion and an axiom print. The source closure contains no proof hole, custom axiom, unsafe/native shortcut or Challenge import. This is source hygiene, not actual execution of those assertions. Original Colbrook/Cambridge mathematics and George Stepaniants/Caltech Computing and Mathematical Sciences formalization attribution remain distinct, with no George email. Exact inputs and primary-source files are retained and bound in CHECKS and MANIFEST. I made no author edit or local Lean/Lake invocation. Later envelope, general bound, witness and final extremum obligations, complete independent reviews, source-matched Linux execution, Comparator/default-kernel, axiom and rejection/sandbox controls remain pending before the problem can count as verified.
