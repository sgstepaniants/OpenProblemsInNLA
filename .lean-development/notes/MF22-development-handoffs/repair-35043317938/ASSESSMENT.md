# GreenKernel proof-only repair for actual Linux 35043317938

The authentic module diagnostics reported two residual goals. In the initial boundary proof the default simplifier removed the leading identity matrix before the four-product entry lemma could match. An explicit restricted simplification phase now retains that identity through the entry rewrite, after which the nonzero row index gives zero. In the terminal boundary proof, denominator clearing left an entry times (1 - 1); ring normalization finishes it. Every declaration signature and the complete frozen boundary remain unchanged. The failed source, raw log and exact diff are retained. This is author source preparation only; repaired Linux execution and final acceptance are pending. No local Lean/Lake or push occurred.
