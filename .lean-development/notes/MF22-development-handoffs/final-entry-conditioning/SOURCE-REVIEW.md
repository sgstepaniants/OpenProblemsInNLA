# MF-22 final uniform-entry and conditioning source handoff

This is an author source/API assessment, not an independent review or actual Lean acceptance. The preceding five repaired modules, three peer projector modules and seven Green/dominant/tail modules are held in root's separate closed batch. These five files are reserved for a subsequent coherent batch.

GreenCoefficients bounds only ratios of natural powers with ordered exponents. The actual inverse boundary is normalized by gamma * mu^n. Its norm is at least one half in the eventual range. Every mixed coefficient is at most 2 / norm(gamma). In the forward range ell < j, the leading difference is exactly mu^(j-1-ell) * (1 - d_n^-1), so the quantitative scalar-tail bound cancels its growth. In the other range j <= ell, the summed power is at most n. The common bound D + 2 is independent of all running indices.

GreenEntryAlgebra expands the genuine rank-one boundary sandwich before estimating. The exact Green kernel is five terms: the cancelled leading coefficient times P*G, a conditional bounded remainder, and three mixed remainder terms. Each product estimate has inner dimension four and uses the literal boundary matrix. Its majorant depends only on fixed parameter/root data and the already proved uniform remainder constants.

GreenEntryBounds discharges every spectral premise using four_roots, dominant_projector and spectral_tail_bounds. It quantifies over every n in the eventual range, every j <= n, every ell < n, and all four-by-two entries. The final theorem is precisely the frozen uniform_green_entries statement, including actual nonzero boundary scalar. There is no sampled dimension or selected subdomain.

Conditioning uses green_inverse to obtain the true inverse, restores the original factor 80, and bounds all entries of the original matrix inverse. The actual cardinality is 2n and the norm is the frozen complex Euclidean operator norm. Multiplying the two complete entry-to-norm bounds gives the positive quadratic coefficient 4(2+rho)B. The original existence target is proved with real exponent 2; no sharper exponent is claimed.

All 22 final theorem signatures match the frozen Challenge after whitespace normalization. Solution imports only the implementation and asserts kernel-only LeanCert trust and prints axioms for all 22. All 29 definition/proof/entrypoint files are free of proof placeholders, added axioms and native_decide. Pinned API inspection confirms power-order inequalities, actual reciprocal normalization, finite matrix products, ENNReal.ofReal_le_ofReal and Real.rpow_two. No local Lean/Lake/cache work, shared-worktree mutation or push occurred. Full compilation, independent complete proof review, Comparator, kernel replay and control gates remain pending.
