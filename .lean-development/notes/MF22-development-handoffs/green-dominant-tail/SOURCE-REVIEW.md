# MF-22 Green reconstruction and spectral tail: author source handoff

Seven complete proof-body modules supply five exact frozen exports and supporting identities. This handoff is an author/integration source check, not an independent referee report or successful Lean execution.

GreenKernel proves the actual fixed transfer recurrence, zero initial components, and the terminal first component when the actual boundary scalar is nonzero. StateReconstruction explicitly handles the original zero extension at j = 0, j = 1 and j = n. GreenInverse reconstructs every forced source state for every right-hand side, proves the actual scaled matrix has the displayed two-sided inverse, and restores the original factor 80. No finite matrix inverse is assumed.

EigenEvaluation proves polynomial evaluation identities on a left or right eigenmatrix by polynomial and power induction. Its pinned Algebra.commutes API is an equality, without a .eq field. SpectralAlgebra discharges the peer's characteristic-polynomial hypothesis using the actual determinant certificate. Dominant derives nonvanishing of the actual spectral boundary entry from the actual adjugate, left/right projection, the peer's simple-root rank-one sandwich, cyclic trace, and the proved numerator/denominator coprimality. SpectralTailBounds controls all natural powers of the three nondominant modes using the genuine Euclidean operator norm, then applies the exact geometric-growth scalar bound to the actual boundary scalar; it includes the unconditional frozen spectral_tail_bounds export.

Source API inspection includes Matrix.mulVec_mulVec, ext_iff_mulVec, mul_adjugate, adjugate_mul, inv_eq_right_inv, det_ne_zero_of_right_inverse, determinant scaling, cyclic trace, Matrix.ite_apply, polynomial induction and aeval_monomial. No local Lean/Lake execution, cache/dependency operation, source-target weakening, workflow mutation or push occurred. All five frozen signatures match literally after whitespace normalization; the complete frozen file map remains unchanged. Uniform Green entry estimates and final conditioning assembly are still being implemented. All seven modules need actual Linux compilation and later independent full source/mechanical review.
