# MF-22: observed final-wrapper repair after run 35050121652

The actual failed component at Git `b428c974e7d82b37a8c11ad7e8cb5dc4ebfb3380` compiles every prerequisite through GreenEntryBounds and the first two Conditioning exports. Its only source error is the final `polynomial_conditioning` wrapper: the already-proved inequality has natural square notation, while the target explicitly calls `Real.rpow` with real exponent 2. LeanCert correctly rejects the dependent `sorryAx` introduced by that elaboration failure. No complete graph acceptance is claimed.

The one-line proof-only correction adds `Real.rpow_eq_pow` before the existing `Real.rpow_two` in `simpa only`. Pinned Mathlib `Analysis/SpecialFunctions/Pow/Real.lean:41` proves `rpow x y = x ^ y` by `rfl`; line 470 proves `x ^ (2 : Real) = x ^ 2`. The two rewrites convert the explicit target to precisely the natural-square inequality already proved. They require no sign or invertibility assumption. The witness exponent remains exactly 2 and the original existential real-exponent target is unchanged.

All 29 original proof-file bytes match the authentic run's pre/post receipt, and Conditioning matches the actual Git blob. Exactly this proof line changes; every declaration header, other 28 files and all 10 frozen inputs are unchanged. All 22 frozen contracts therefore retain their existing reviews. The complete failed log, receipt, root audit, Git before-source, Mathlib source, diff and new source closure are retained. The new proof bytes require independent diff review and actual Linux recompilation; no local Lean or Lake was run.
