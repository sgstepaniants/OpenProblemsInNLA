# MF-22 proof-only repair assessment for Linux run 35041873518

These five files repair concrete elaboration failures from the retained authentic module log. No frozen statement, definition, quantifier, constant, finite-domain boundary, source provenance or dependency pin changes. This is source inspection and candidate preparation, not a successful Lean execution or final mathematical referee report.

- ScalarTail: use numerical contradiction normalization for the impossible real inequality at d = 0, and finish a denominator-clearing identity by commutative ring normalization.
- Coprime: allow the conjugation rewrite phase to make no changes in its conjugation-free first branch. Clear a quadratic rational evaluation with an abstract nonzero denominator before expanding the actual parameter polynomial; the new helper is the exact field identity d²(a + b(u/d) + c(u/d)²) = ad² + bud + cu². Resolve the already-produced disjunction with rho > 0 instead of passing that disjunction to nonlinear arithmetic. The exact elimination coefficients and all parameter cases are unchanged.
- SourceRecurrence: expose the concrete finite coordinate 0 or 1 before row rewrites, and explicitly unfold the local right-hand-side abbreviation b in four vector-coordinate bridges.
- RootCayley: the contributing agent adds the literal roots abbreviation to the index-zero simplification only.
- TransferCertificates: the contributing agent adds finite-index equality/order normalization to two fixed minors, and unfolds the actual leadingBlock in two inverse-cancellation row proofs. The determinant route and matrix dimensions are unchanged.

Both peer diffs were read against their preserved failed sources and authentic diagnostics before exact-byte integration. Existing declarations have identical textual signatures after whitespace normalization; the only new declaration is the generic quadratic denominator identity. No local Lean, Lake, cache operation, workflow edit, commit or push was performed. Full rerun, independent final proof review, Comparator and kernel-replay acceptance remain pending.
