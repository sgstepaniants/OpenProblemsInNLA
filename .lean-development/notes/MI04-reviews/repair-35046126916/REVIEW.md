# Independent MI-04 repair addendum for run 35046126916

Reviewer: OpenAI Codex agent `/root/next_matrix_functions`, an MI-04 nonimplementer. Verdict: **APPROVE the four source-level proof reductions in Rayleigh and Coordinates, and the two proactive disequality-namespace replacements in Probes.** This preserves the limited scope of my prefix, exact-sandwich and weighted-probes reviews; it is not complete MI-04 acceptance or acceptance of the repaired files by Lean.

I read all three complete repaired files, their exact diffs and the authentic Linux diagnostic log at commit `64a04495cb03fa7496cbdc3dc72f94779ab4c340`. Independently recomputed packet hashes and `git show` comparisons establish that the Rayleigh and Coordinates before files are exactly the failed inputs. Probes before bytes exactly match my earlier immutable weighted-probes source review. All covered lemma, theorem and definition headers remain unchanged, and all ten frozen files match the statement freeze. The checks and retained snapshots bind the precise reviewed bytes.

In `Rayleigh.shifted_topValue`, the logged goal is c + Q(v) <= c + topValue(M). Replacing the incorrectly oriented additive helper with `add_le_add (le_refl c) ...` supplies exactly this bound from the existing all-unit-vector inequality. No shift sign assumption or variational restriction is added.

In `Coordinates.realQuadratic_add_vectors`, the existing equality of real parts from `inner_re_symm` is changed explicitly to `Complex.re` on both sides. This is the intended definitional reduction of the real-like scalar projection on complex numbers, allowing linear arithmetic to use the same Hermitian cross-term identity. It neither identifies conjugate complex inner products nor assumes their imaginary parts vanish.

In `realQuadratic_real_perturbed_vector`, the complex-linear map and right argument of the complex inner product are scaled before reducing the real part of the real scalar epsilon. The local `hcross` proves Re(<v,M(epsilon w)>) = epsilon Re(<v,Mw>) using actual complex linearity. The unchanged squared-norm scaling gives epsilon squared for the pure w term. This retains every real epsilon, arbitrary vectors and the exact second-order expression without an approximation or additional condition.

For a real diagonal matrix, `Ne.symm h` is the explicitly named reversed off-diagonal disequality. It eliminates the same reversed equality branch that the invalid field projection `h.symm` was intended to eliminate. Real diagonal entries and the Hermitian conclusion are unchanged.

The two Probes replacements apply the identical `Ne.symm` correction to the row and column uses of `weighted_probe_difference`. After the diagonal case is closed, hij means i != j, and both exact probes require j != i. The corrected explicit lemma supplies precisely that fact; coefficients, sums, indices and the final norm-square identity are unchanged. Probes has not yet been compiled: this is a predictive API repair informed by the actual Coordinates failure, not a reported Probes compiler failure or runtime pass.

The prior actual run accepted `Quadratic` at its already reviewed repaired bytes and `ScalarGeometry`, with standard axiom prints for their covered exports. It failed `Rayleigh` and `Coordinates`, and its trust checks correctly rejected the resulting dependent placeholders. Those failures are retained. The present repaired files still require a real Linux build; the dependent sandwich, full problem, Comparator, default-kernel replay and rejection controls remain separate pending gates. A separate ongoing Geometry source revision appears in the author's current inventory and is explicitly outside this bounded addendum; no claim is made that the entire active project is unchanged or reviewed. I ran no Lean or Lake process and changed no author file.
