# MI-04: independent source review through normality

**Verdict: approve the four intermediate mathematical proofs at the exact bytes in `CHECKS.json`, subject to actual compilation and the complete-project gates.** This is an incremental source review by OpenAI Codex agent `/root/next_elimination`, who has not implemented MI-04. It is not full MI-04 acceptance, human peer review, or certification of the checking software.

I read all of `WeightedProbes`, `Probes`, `BasisTransport`, and `Normality`, the frozen definitions and all 21 contracts, the original canonical target and complete Colbrook manuscript, and the relevant dependency arguments. The earlier second-order-limit and block-extrema source reviews remain unchanged and are retained here. The present review reconciles the explicit later repairs to `Rayleigh`, `Coordinates`, and `Probes`; it does not silently replace their earlier snapshots.

## Mathematical correspondence and non-vacuity

1. `weighted_magnitude_identity` uses the proved Rayleigh sandwich limit at two actual, simple top diagonal entries. The left block peak gives the row coefficients because the lower-left block is the conjugate transpose; the right peak gives the column coefficients. Every denominator is the required `1 + d j`, including the diagonal denominator two. Positive scaling and the sign unitary identify the two actual upper extrema for every positive perturbation. Uniqueness of the limit on the nontrivial right-neighborhood filter therefore compares their coefficients. Neither an assumed eigenvalue branch nor an unproved perturbation expansion is introduced.
2. `offDiagonal_magnitude_symmetry` compares the exact diagonal probes with the selected off-diagonal coordinate respectively zero and one half. Their difference is precisely one third of the chosen squared magnitude. All other finite-sum terms cancel; the diagonal case is treated directly. The fixed choices fit the already proved gap bounds, so no dimension-dependent interval computation is needed.
3. `orthonormal_pair_symmetry` extends each arbitrary complex orthonormal pair to a genuine basis, reindexes it by `Fin n`, and constructs its actual change-of-basis unitary. The column action and matrix coefficient identities transport the already established entry result to the given vectors. No coordinate-only hypothesis, invertibility assumption, or restriction `2 ≤ n` is substituted. In dimension one the pair condition is appropriately vacuous, while the following singleton argument remains available.
4. `pair_symmetry_normal` extends each individual unit vector to a basis and compares **every** Parseval term. Its own basis coordinate is explicitly included as an equality; the other coordinates use the pair hypothesis. The adjoint identity is applied in the correct complex-inner-product order. Equality of the full squared norms gives equality of operator and adjoint norms on unit vectors, then on all vectors by a separately justified nonzero normalization, with zero handled directly. The actual Mathlib CLM normality criterion and the injective matrix/CLM star-algebra equivalence yield exactly `X.conjTranspose * X = X * X.conjTranspose`.

These arguments retain arbitrary complex matrices, every positive dimension, zero and scalar matrices, and the original singular-completion hypothesis through the previously reviewed bridges. They prove the intended intermediate steps of the necessity direction. They do not yet establish the remaining eigenvalue collinearity, affine Hermitian representation, or the complete final implication.

## Source/API finding and repairs

I found one prospective API defect in the original `BasisTransport.lean:48`: `hne.symm` was used with `hne : u ≠ v`. The authentic earlier `Coordinates` compiler diagnostic shows that this disequality field lookup fails in the pinned environment. I reported it without editing the author's source. The author supplied a separate packet replacing that one occurrence with `Ne.symm hne`. I independently checked the exact before/after bytes: this is the sole change, no declaration type or mathematical premise changed, and the corrected file has SHA256 `a3bd7f091903150bcf01c981a5a1ae8aae7bc8c31ea00f4b601aa53636c9341e`. This source-level finding is closed; compilation of the correction is still pending.

The separate `Probes` packet makes only two analogous `Ne.symm` substitutions. The `Rayleigh` and `Coordinates` packet is bound to failed run `35046126916` at `64a04495cb03fa7496cbdc3dc72f94779ab4c340`: its original files match that exact Git commit. Its changes disambiguate real-part normalization, a real-scalar cross term, addition monotonicity, and disequality symmetry. I checked each complete diff against the corresponding actual residual goal. They preserve the earlier mathematical arguments and all frozen signatures. The new four modules are not asserted to have executed in that failed run.

## Scoped Tau Ceti and trust checks

The original source is retained from immutable upstream `ce47b5630bf3680d9211131c3a43825b022c139a`; all inspected primary Mathlib APIs are bound to `0df444a360eaa60ab8c11dca51a86af692955474`. In particular I checked the genuine orthonormal-basis extension and reindexing, finite Euclidean dimension, Parseval identity, unitary basis matrix, CLM adjoint inner-product formula and norm criterion, matrix star-algebra equivalence, eventual-function equality, and Hausdorff limit uniqueness.

All ten frozen files still match the statement freeze. The four new theorem headers match the independent Challenge literally up to whitespace. The 15 retained implementation modules contain no proof holes, new axioms, unsafe/native proof paths, or Challenge imports. Each of the four exports has a LeanCert kernel trust assertion and axiom-print command in source. These are prospective checks, not measured acceptance. No local Lean or Lake was run, no fresh default-kernel replay or Comparator execution was inspected for these modules, and no transitive axiom list is certified here.

`audit_inputs.py` reproduces the source binding and conservative textual checks, retains the full relevant before/after/diff and actual diagnostic, and validates immutable Git provenance without invoking Lean. The formalization remains credited to George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology; the original mathematics remains credited to Matthew J. Colbrook, University of Cambridge DAMTP. No email is published.
