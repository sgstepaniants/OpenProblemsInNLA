# IE-13 independent root complete-source review

**Approve the complete mathematical candidate for actual Linux checking.**
There is no unresolved mathematical finding on the exact27 source files in
CHECKS.json. Compilation, Comparator, default-kernel replay and final runtime
reconciliation remain pending. This verdict increments no verified count.

Reviewer: root Codex AI agent. I did not implement IE-13. I reviewed its original
target, full retained manuscript and numerical-first statements, then cumulatively
read its complete implementation across this campaign's source-review turns.
In this continuation I read the new rational factorization and final attainment
modules, the complete updated definitions and all repair diffs. Prior independent
reports are supplementary; this verdict is based on the mathematical source.

The final result quantifies all unequal natural bandwidths and the real set of
growth values from every permitted dimension, every nonsingular complex input
with those original-order bands and every legal maximum-modulus pivot path.
The literal swaps and Schur updates, all active entries at all stages, input and
last scalar, are preserved. Definitions contain no invertibility, bound or
attainment assumption disguised as part of the data. Entry and active maxima
are actual finite maxima. Positivity of the input maximum follows from the
nonzero determinant. Existence and prefix extension are proved for genuine GEPP.

The upper estimate follows actual original-row labels. The proof bounds old
survivors by p, includes the fresh boundary row and bottom truncation, and proves
future rows untouched. Arbitrary-subset sums control every cardinality of the
front, with multiplier norms at most one for every allowed pivot tie. The exact
history register proves the required recurrence and zero before the start.
Early columns start with the one-update envelope; late columns start with zero
until their actual front can reach them. Column age is bounded by p+q before
the column leaves the active matrix. A singleton-subset estimate plus untouched
row bounds yields every active entry bound and hence the true growth maximum.
For p=0 the front has no old survivors and the proof gives growth exactly one.

The attaining input is defined over the rationals in its original ordering and
then embedded in the complex field. Exact finite geometric sums establish the
early-column support and magnitude. The scale (1/2)^p is positive and cancels
2^p symbolically; the LeanCert kernel certificate for the scalar half is used
in that proof. The target column supplies an entry one, so the input maximum is
exactly one. The dimensions obey the canonical minimum without imposing a cap.

The explicit permutation is a factor-row labeling of the defined input, not a
preliminary algorithmic permutation. The actual physical seed swaps produce the
proved rotation prefix. The complete lower and upper factors multiply to that
input, column by column, including the target and later identity columns.
The forward recurrence is proved by exact finite-window reindexing. Every upper
diagonal entry is a nonzero positive rational and the lower diagonal is one.
This proves nonsingularity of the entire input rather than assuming it.

The relabeled-LU trajectory lemma is used with all its hypotheses proved for
these factors. The prefix multipliers have modulus at most one, so the prescribed
prefix is admissible for the actual original matrix. At stage p+q, the target
diagonal equals the full recurrence h_p(p+q). Only this prefix is prescribed:
the existing extension theorem supplies genuine legal later pivots and preserves
the target stage. The unit input maximum and universal bound force exact growth
equality. The p=0 endpoint uses an identity matrix of dimension q+1 with a proved
literal no-swap admissible path. These witnesses give a member of the genuine
growth set at its upper bound, yielding IsGreatest and the actual real supremum.
The cases p=1, q=0, arbitrary p>0 and the zero-lower-band endpoint remain included.

The actual35052024095 log is a failure, not proof acceptance. Its successful
basic/prefix modules and its precise Recurrence/Front diagnostics are retained.
The current repairs only instantiate a finite sum, expose set coercions, fix
tactic syntax, project Fin order to natural order, supply decidability and
normalize a cardinality equality. The analogous UpperBound projection is a
clearly labeled predictive repair, not a fabricated diagnostic. I requested it
after inspecting the exact types; the implementation author made the change.
None changes a public statement or frozen data. The author's initial repair
audit had an empty source-binding map; the mandatory retained supplement fixes
that audit, and I independently checked all14 before-sources against actual Git
and the failed-run receipt. The original erroneous empty map is not evidence.

My executed checks rehash all27 sources, all10 frozen inputs and original
snapshots, every packet file, the implementation map and all28 explicit public
signatures. There are no extra section instances in these modules. All28 exports
are listed for kernel trust and axiom output. This textual fidelity check does
not substitute for actual elaborated-type comparison by Comparator.

This review covers the repository's scoped Tau Ceti fidelity, correctness,
generality, proof quality, computation reduction, reuse, API, documentation,
placement and attribution criteria. The accepted IE14 literal GEPP infrastructure
and pinned Mathlib matrix, finite-set, determinant and order facts are reused;
the problem-specific upper bound and attaining family are proved here. There
is no parameter grid, dimension enumeration or numerical spectral computation.
Original mathematical authorship remains Matthew J. Colbrook, Cambridge DAMTP;
formalization is George Stepaniants, Department of Computing and Mathematical
Sciences, California Institute of Technology, with AI assistance and no email.
This is independent AI-agent review, not official Tau Ceti or human certification.
