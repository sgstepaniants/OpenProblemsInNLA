# IE-13 complete independent mathematical source review

Reviewer: OpenAI Codex agent `/root/mi04_independent_referee`. I did not implement or edit any IE-13 proof. This is an independent AI review of the complete candidate, not a claim of external human peer review or formal verification.

**Verdict: approve the exact complete source candidate for full Linux compilation and Comparator verification.** I found no substantive mathematical gap or weakened target. This approval includes the observed Recurrence and Front repairs, their corrected source-binding supplement, the earlier SubsetAlgebra repair, and the latest predictive UpperBound order projection. All execution gates on the complete revised source remain pending. No verified count increases from this report.

The complete author packet is `d4d3ec6a95ace43a1415d0a9cde1fd0c68165c1c18dcce5486fcb00d9b01f946`, followed by predictive packet `a64d867e817feb6bd71e431305ffed02bd05e289ae716490bea438396fddf6c7`. The current 27-source map is retained in CHECKS.json and INPUTS.json and bound to implementation map `496461472a295e32c8e92242213640d311d21993489babaeb12fb26c484356a1`. The frozen statement boundary is `85916aae89eb2111ff914b8767e8918548c132a6ff6c7ed92e52286bec1998fe`. I read all 27 implementation files, all 28 Challenge contracts, Definitions, numerical targets and correspondence, the complete original canonical README and the full mathematical manuscript.

## Original target and semantic fidelity

I checked the canonical IE-13 page and Colbrook manuscript against their actual Git blobs at upstream commit `ce47b5630bf3680d9211131c3a43825b022c139a`. Their hashes are respectively `2977da8c5a1144e8bec75b77eeb6df1ed9ac49ffc3ab741a3f278e6310088d6b` and `cc2fd88017d3f372cd41682d3ea06b390786a39cc0552267c77a3211250b485a`. The target is the least universal GEPP element-growth bound for original lower/upper bandwidths p and q, positive allowed finite dimension, nonsingular complex input, and every legal maximal-modulus pivot choice. The published unequal-bandwidth condition remains in the final theorem. Auxiliary results cover equal bandwidths too; this strengthens the argument without substituting another target.

Definitions implements actual row interchanges followed by the strict trailing Schur update. Admissibility requires an active nonzero pivot with modulus at least every active entry in its column. Growth uses the input maximum and every active entry in all n stages, including the original matrix and last scalar. Original-row labels are carried by the recursively composed physical swap permutation. The growth-value set really ranges over these dimensions, matrices and paths. Neither a custom definition nor a hypothesis encodes the desired inequality. Greatest-value and real-supremum conclusions are supplied for this actual set.

The claimed answer is 1 when p=0, and h_p(p+q) when p>0, with h_p(0)=0 and h_p(t+1)=1+sum over r in Fin p of h_p(t-r). Natural subtraction correctly provides the zero-history convention. The source proves a maximum, which is stronger than merely producing arbitrarily close examples.

## Universal estimate

Basic and Maxima establish the finite maximum semantics and positivity of the input denominator. GEPP proves active injectivity from input nonsingularity and preservation by each Schur update; it thereby constructs a legal maximum-modulus path. Prefix proves that any legal finite prefix can be extended while preserving its actual states. These are real elimination arguments rather than assumptions that a desired suffix exists.

Origin and Front track surviving original labels under each selected pivot. Rows sufficiently far below the current front are still untouched because their pivot-column entries vanish. The pivot must belong to the front, and deleting its label leaves at most p old rows. This handles missing new rows near the bottom and does not presume that physical row order remains the original order.

Recurrence and Envelope give the exact symbolic history and p-entry envelope. SubsetAlgebra proves the required norm-sum inequality for every finite subset of old/front rows, with at most one fresh input-size entry. Removing any selected pivot and using each multiplier's modulus at most one yields the next envelope. Thus the proof covers arbitrary legal ties, rather than choosing a favorable pivot for the universal bound. The saturated p-entry case and nonsaturated smaller subsets are both treated.

LateColumn, FrontBounds and ColumnBounds prove the zero-history phase and its first nonzero transition, then the envelope bound at the correct column age. For an active column this age is at most p+q. UpperBound treats an old row through the singleton envelope estimate and any remaining active row through the untouched-future-row bound. Hence every active entry is bounded. For p=0 there are no old rows, and the initial stage forces growth exactly 1. No dimension cutoff, real-only restriction, special path, or exclusion of q=0 enters the universal conclusion.

## Rational attaining witness

WitnessScale, WitnessOrder and TailAlgebra provide the scale, literal row-swapping order, and a generic factorization-to-trajectory bridge. WitnessColumns, WitnessEntries, WitnessForward, WitnessFactors and WitnessProduct establish the entire rational construction before the final five modules assemble attainment.

The witness has N=2p+q+1 and target index p+q. Its small columns are scaled by the exact rational (1/2)^p; finite geometric-sum cancellation gives original support within the prescribed band and input-entry modulus at most one. A literal target-column entry equals one, so the input maximum is exactly one. These are statements about the original frozen matrix, not its permuted factorization. Dimension admissibility is proved as well.

The prescribed prefix actually swaps physical positions, with the pivot initially at position p. WitnessOrder explicitly computes the rotation of original labels and proves the factor-row index at each selected pivot. The LU trajectory bridge uses those identities inside the literal Schur recurrence; it does not silently reorder the input first.

WitnessForward reindexes the lower-band window and proves forward substitution with first value 1 and subsequent values h_p(i). WitnessFactors constructs a complete lower factor, including the below-target residual divided by the positive target diagonal, and a complete upper factor. WitnessProduct proves their product equals the actual rational input under the explicitly known row permutation. Unit lower diagonal and positive upper diagonal prove nonsingularity by triangular determinants. Only prefix lower multipliers need modulus at most one; no unsupported assertion about the prescribed suffix is used.

WitnessDynamics obtains the legal prefix and target entry h_p(p+q). Attainment then invokes the independent legal-prefix extension theorem; preservation of the target stage forces growth at least the proposed bound, and the universal estimate gives equality. Identity supplies the p=0 endpoint in dimension q+1 through actual identity elimination. Sharp obtains membership and an upper bound for every value in the genuine growth set, hence IsGreatest and the real supremum. I checked the q=0 endpoint, p=1, the p=0 identity case, smallest admissible dimensions, the last scalar, and ties. Empty finite sums are handled within the symbolic arguments.

## Exact formal boundary and repair review

I independently checked all 28 declaration headers against the frozen Challenge, including binder types and order. Unlike MI-04's earlier issue, neither IE-13's Challenge nor any implementation source has ambient `variable`, `include`, or `omit` declarations; therefore no extra section-instance mismatch is hidden by the textual header comparison. This remains a source-level check, not an executed Comparator result. All ten immutable files match both the statement snapshots and the current candidate. All 27 sources are reachable from Solution. The 28 final trust assertions, 28 axiom-print commands and Comparator names agree, with only propext, Classical.choice and Quot.sound permitted.

I read the complete actual IE-13 compiler log `36992feebc6331a636b707f44cb79fa33e85c5465715472b8f95f30cbdde2cb8` from failed Linux run 35052024095 at commit `93abf01922d103993f2a92e819f207d63effbfae`. It accepted Basic, Maxima, GEPP, Prefix and Origin. Recurrence failed at the unspecified function in the range-sum rewrite. Front failed at finite-set coercion, tactic syntax, Fin-to-Nat order projection, classical filter decidability and normalization of the erased-front cardinality. Its trust command rejected the resulting sorry dependency. This is a correctly reported failed run, not evidence of complete acceptance.

The repaired range rewrite supplies the precise natural-valued function to Finset.sum_range. Front's repairs supply the exact Set coercions, ordinary tactic sequencing, Fin.le_iff_val_le_val projection, classical decidability and the definitional index normalization. All declaration headers are unchanged. The UpperBound projection is the same explicit order conversion; its diagnostic was predicted from the actual Front error, not observed in an UpperBound run. The earlier SubsetAlgebra change uses add_le_add with the common summand explicitly on the left. I checked these repairs against pinned primary Mathlib sources.

The first author's repair record had an empty source-binding map. I did not accept that empty map as evidence. I independently checked the later 14-entry supplement against the actual Git blobs and receipt. In addition, the audit compared all 1118 receipt source inputs to the failed commit, all nine command before/after maps, all nine log hashes and their actual artifact ZIP members. It also checked seven immutable author packet manifests, all original-source Git hashes, exact reused IE-14 source provenance, and twelve pinned primary API files. These operations invoke only Python and read-only Git.

## Scoped referee standards, computation, and limits

This review applies the repository's Tau Ceti adaptation in retained docs/lean/REVIEW.md: correctness, target fidelity, degenerate cases, generality, imported API reuse, proof quality, placement, naming, documentation and attribution. It is not an official Tau Ceti service verdict. The generic tail identities have retained IE-14 provenance; the new arbitrary-prefix bridge is separately proved. George Stepaniants's formalization credit and Caltech department/university affiliation are present, with Colbrook's mathematical attribution preserved and no email published in these proof headers.

Computation has been reduced to a single consumed LeanCert kernel-mode certificate for 0<1/2<=1. It supports positivity and upper bounds for every symbolic power; no matrix enumeration, interval subdivision or parameter cutoff supplies the argument. The remaining recurrence, finite-set, matrix and determinant proofs are symbolic. Source screening found no implementation placeholders, new axioms, unsafe/native proof shortcuts, custom elaborators or Challenge imports. The Challenge's deliberate holes are specifications only.

Fresh full Linux compilation, transitive axiom auditing, actual sandboxed Comparator comparison and its controls, a second independent final referee, and the canonical exact-published-commit workflow remain separate required gates. This reviewer ran no local Lean/Lake and edited no author proof. This source verdict must not be presented as a completed Lean verification.
