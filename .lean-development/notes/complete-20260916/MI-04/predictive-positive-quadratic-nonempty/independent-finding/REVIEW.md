# MI-04 independent continuation referee

**Verdict: the complete mathematical route and the two observed `neg_ite` repairs are approved at source level. One formal statement-matching correction is requested before final formalization approval. Actual complete Linux execution and canonical verification remain pending.**

Reviewer: OpenAI Codex AI agent `/root/mi04_independent_referee`. I have not implemented MI-04 and made no changes to its author sources. This is an independent AI source review under the repository's scoped Tau Ceti protocol, not external human peer review, an official Tau Ceti service result, or certification of Lean itself.

I read all 21 current implementation files in full, including Definitions and Solution; all 21 Challenge contracts; the frozen numerical obligations and source correspondence; the original canonical question and complete Colbrook proof; and the full actual MI-04 compiler log from Linux run `35052024095`. I separately inspected the main pinned Mathlib APIs and the primary Lean elaborator rule relevant to the finding below. Prior referee reports were read after the full current implementation and are not substitutes for this review.

The reviewed closure is `6f0865308ec97f9052f862e54a94ca4c439ca426742d88ab617c267a841d9ddc`, with exact per-file hashes in `CHECKS.json`. The latest observed-repair packet is `5787a22e4421a0c326bf363d227f151913d20b6f94f3dea19ce6dd07fd6feae7`. The active source and ten frozen inputs are retained as text snapshots in `reviewed-source/`; the original manuscript, pinned APIs, exact repair, and real failed execution evidence are retained separately. None is replaced by a success claim.

## Required formal-fidelity correction

`Challenge.lean` declares `positive_quadratic_iff` inside a section with `[Fintype ι] [DecidableEq ι] [Nonempty ι]`. Its implementation, `NLA/MI04/Quadratic.lean`, has only the first two instances in scope. These explicit theorem bodies describe the same matrix assertion, and the implementation is mathematically stronger because it also covers an empty index type. Nevertheless, their elaborated types are expected to differ by the implicit `Nonempty ι` argument.

This is not based solely on general memory of Lean. The installed Lean 4.33.1 source `Lean/Elab/MutualDef.lean`, lines 445–488, explicitly includes instance-implicit section variables whose dependencies are included. Its lines 568–579 suppress the unused-section-variable warning when a declaration has `sorry`, explaining why the Challenge log does not warn. The actual MI-04 implementation log independently exhibits this same rule: `positive_real_scalar` is warned for automatically including an unused `[Nonempty ι]`.

The primary elaborator file is retained at `primary-api/installed-lean-4.33.1/Lean/Elab/MutualDef.lean.txt`. It is a read-only snapshot of the installed toolchain source, not an independently rebuilt Lean executable. The report does not claim that Comparator has already rejected this mismatch; Comparator has not yet run on the candidate. Its configured exact statement-identity requirement makes this a concrete predictive boundary issue to resolve before acceptance.

**Requested correction:** preserve the frozen Challenge and add `[Nonempty ι]` to the exported implementation lemma `positive_quadratic_iff`. Do not add it indiscriminately to unrelated helpers. The canonical target already has `n ≥ 1`; this correction aligns the helper with its approved contract and leaves the canonical theorem unchanged. Bind the change in a separate predictive repair packet, retain this original finding, and obtain actual elaboration and Comparator evidence. I inspected the other 20 exported declarations' section scopes and found no analogous discrepancy.

## Fidelity to the entire original problem

The final theorem retains every natural dimension `n ≥ 1` and every complex square matrix `X`. Its premise quantifies all Hermitian `A,B` whose actual block matrix `[A X; X* B]` is positive semidefinite. Its conclusion produces an actual Hermitian `K` and complex `α,β` with `X = α K + β I`. The proof does not assume normality, diagonalizability, a simple spectrum, nonsingularity, positive definiteness, bounded entries, or the desired representation. All those intermediate structural properties are proved where needed.

`spectralNorm` is the operator norm of `Matrix.toEuclideanCLM` on complex Euclidean space. The norm is not a default entrywise or Frobenius norm. The block index is the full disjoint sum of both index sets. `Matrix.PosSemidef` and `Matrix.IsHermitian` are the genuine library notions. `topValue` is the supremum of actual quadratic forms over all unit vectors; its attainment, order, and norm properties are separate proved bridges. No definition hides a desired theorem or narrows the universal premise.

The Colbrook manuscript proves a stronger four-way equivalence. This project proves its necessity direction, which is exactly the permanent canonical question. Excluding the manuscript's converse from the formalization is therefore a truthful scope choice, not an incomplete solution of the canonical problem.

## Complete mathematical proof review

The compact unit-sphere argument in `Rayleigh` proves attainment, including the nonemptiness supplied by a coordinate vector. The real quadratic form is correctly related to Mathlib's operator convention through symmetry of real inner-product parts. Positivity gives the nonnegative Rayleigh quotients needed to identify the actual operator norm with the top value. Scalar shifts and scalar order work at zero as well as positive shifts.

`BlockSymmetry` applies the original universal assumption to the specific boundary completion `cI + K_X(T)`, where `c = topValue(-K_X(T))`. Opposite diagonal coordinate tests prove `c ≥ 0` without a trace convention. Scalar order proves this actual completion is PSD, including a singular boundary. Its diagonal-block sum is exactly `2cI`. The sign unitary `diag(I,-I)` and replacement of `T` by `-T` give the reverse extremal inequality. Choosing useful completions in this proof does not restrict the original universally quantified hypothesis. The scaling and unitary-similarity arguments explicitly transport arbitrary Hermitian test matrices back by their inverse operations.

The exact second-order argument is sound. For the lower bound, the correction has zero peak coordinate and entries `V_ja/(1-d_j)` elsewhere. Each denominator is positive from `d_j ≤ 1/2`. Direct expansion yields the stated quadratic numerator and the true squared norm `1 + ε²‖w‖²`; the latter is not silently replaced by one. The lower bound follows from the all-vector Rayleigh inequality and division by positive quantities.

For the upper bound, `Punctured` decomposes every vector into its peak coordinate and orthogonal remainder. The operator-norm estimate applies to that remainder. Each cross term has the correct conjugation and column index. `weighted_square_bound` is the exact nonnegativity of a complex norm square divided by a positive real weight. Summing it gives a bound on every unit vector, after justifying the sign of the upper coefficient and the bound on the peak coordinate's norm square. Under the frozen small-positive-ε condition, every modified denominator exceeds `1/4`. The two explicit bounding functions tend to the same second coefficient, and `Limit` squeezes the actual quotient on the right-neighborhood filter. This proves the desired limit without an unproved analytic eigenbranch or asymptotic remainder.

`WeightedProbes` uses the correct two opposite diagonal peaks of the full block pencil. The adjoint block supplies row magnitudes at the first peak; the original block supplies column magnitudes at the second. Both denominators are `1+d_j`, including the distinguished term with denominator two. Positive scaling and extreme symmetry equate the two quotient functions, so uniqueness of the nontrivial one-sided limit yields their coefficient equality. The two rational probes change only one off-diagonal weight from one to two-thirds; their difference isolates exactly a factor one-third. Diagonal entries are treated directly. Actual orthonormal-basis extension and a genuine unitary matrix transport the entry equalities to every complex orthonormal pair.

Normality follows from complete Parseval sums for `Xu` and `X*u` in a basis containing the chosen unit vector. Its own diagonal term is included and is equal directly; the remaining terms use pair symmetry. The zero vector is handled before rescaling every nonzero vector. The pinned CLM adjoint-norm criterion proves normality, and the injective matrix star-algebra equivalence returns the literal equation `X*X = XX*`.

The joint-eigenspace construction retains multiplicity correctly. Its finite index set consists of actual eigenvalues of the two commuting self-adjoint parts. Restricting the complete joint-eigenspace supremum to these finite sets removes only bottom spaces, explicitly proved in `JointBasis`. The subordinate orthonormal basis takes a full basis of every intersection, not one vector per eigenvalue pair. Its total dimension is exactly `n`, and each vector satisfies both eigenvector equations. The real/imaginary decomposition then supplies a full complex eigenbasis for the original normal operator. Its basis matrix is proved unitary, and the column equation yields `X = U diag(z) U*`. Repeated eigenvalues, zero eigenvalues, and singular matrices are included.

Finally, `Geometry` extends the pair property to arbitrary orthogonal vectors by homogeneity, handling zero vectors first. The sparse vectors `(1,1,1)` and `(1,I,-1-I)` are exactly orthogonal. Their two coefficients are `a+Ib` and `a-Ib`; equality of norm squares forces `Im(a conjugate(b)) = 0`. Repeated indices are separated before this triple construction. The affine-line step treats coincident values by taking `α=0`; otherwise it proves a selected difference nonzero before dividing. Every quotient is real by the orientation identity. Conjugating the real affine-coordinate diagonal by the actual unitary produces the required Hermitian `K`, and direct matrix algebra proves the explicit final representation.

I found no mathematical gap or vacuity in this route. Dimensions one and two, zero/scalar matrices, arbitrary spectral multiplicity, singular completions, and the zero-shift boundary are retained.

## Observed compiler repair and exact evidence

I read the entire actual MI-04 log, including its failures. Run `35052024095` checked Git commit `93abf01922d103993f2a92e819f207d63effbfae` as Linux UID 1001. It successfully builds `Limit` and prints only the three permitted axioms for `simple_peak_second_order`. It fails on two diagonal-entry identities in `WeightedProbes`, at lines 26 and 32; the dependent export then contains `sorryAx`, which the trust assertion correctly rejects. Later dependent modules and the final assembly have not passed in that run.

The held repair adds `neg_ite` to those two existing simplifier lists and nothing else. The pinned lemma is exactly negation distributed through a conditional, proved by case split and reflexivity. The existing simplifier handles `-0`. The concrete logged residual goals are precisely its instances, so this is an appropriate proof-body repair. It changes no diagonal data, weighted identity, statement, assumption, or coefficient. All auxiliary and public headers are unchanged. The corrected `WeightedProbes.lean` has SHA-256 `e72d7cd3a10c574477a794d26aba66d40da7486c9ead38e9e4e1d31840570eea`.

My independently executed Python audit rehashed the exact packet, all 21 current and prior sources, all ten frozen files against both the freeze and snapshots, the eight immutable original-source files against Git, all 1,118 failed-run Git inputs, all nine command log hashes against the artifact, and every pre/post source map. It compared the primary API snapshots to the exact Mathlib commit. The audit does not invoke Lean or Lake and does not treat the failed run as accepted. It also confirms the implementation import graph is complete and never imports Challenge, all advertised exports are listed for Comparator and kernel assertions, and source scans find no proof holes, added axioms, unsafe/native proof path, or custom elaborator overrides. The explicit 21 headers match after unused-binder-name normalization, but that narrow test intentionally does not erase the section-instance finding above.

## Computation, reuse, attribution, and remaining gates

The proof avoids numerical eigenvalue computation, parameter subdivisions, and dimension-dependent searches. Its main work is symbolic finite algebra and universal variational estimates. Homogeneity removes even the planned square-root normalizations. LeanCert kernel mode proves the single exact rational certificate `0 < 1/4`; that certificate is actually consumed by both denominator-positivity paths. The real failed-run log confirms that certificate and its sandwich consumer built. All advertised final kernel assertions still require successful execution of the complete corrected graph.

The construction reuses pinned Mathlib compactness, genuine Euclidean matrix operators, Rayleigh bounds, PSD order, unitaries, orthonormal-basis extension, Parseval, adjoints, finite eigenvalue sets, joint eigenspaces, and real/imaginary normality APIs. Helpers follow their mathematical roles, and explanatory comments describe the substantial replacements for informal perturbation theory. Minor existing linter warnings do not alter meaning; no linter or trust safeguard is disabled. The scoped Tau Ceti correctness, fidelity, generality, proof-quality, reuse, API, naming/placement, documentation, and attribution angles are addressed here.

Original mathematics remains attributed to Matthew J. Colbrook, Department of Applied Mathematics and Theoretical Physics, University of Cambridge. Formalization remains credited to George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology, with AI assistance disclosed and without his email. Historical preparation labels in frozen documents should remain historical; active publication metadata must state only measured results.

Resolve the section-instance issue, retain a precise addendum on the changed source, and complete the actual full Linux build, fresh default-kernel replay, Comparator identity/axiom checks, sandbox and rejection controls, second-referee reconciliation, and exact published-commit audit before counting MI-04 as Lean verified. This report changes no canonical status or verified count.
