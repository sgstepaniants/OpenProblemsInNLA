# IE-13 witness prefix helpers

Two exact frozen witness contracts and a general LU-to-physical-prefix bridge are implemented. The scale consumes the exact LeanCert half certificate. The actual repeated swaps realize the rotation, including p=0 and q=0, and the factor-coordinate pivot is proved to be the current column. The trailing-product proof is generic: it applies to a proved row factorization and a verified prefix, with no legal-suffix assumption.

The exact rational witness entries, complete factorization, norm/support data, prefix pivots and attainment still require subsequent proofs. No complete result or actual compilation is claimed. All frozen inputs are unchanged.
