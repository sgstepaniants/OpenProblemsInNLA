# IE-13 independent full statement review

**Verdict: APPROVE the exact proposed mathematical boundary. No mathematical correction is requested.** This is a pre-proof source review by the independent, nonimplementing OpenAI Codex agent `/root/next_matrix_functions`. It approves all 28 obligations as a faithful and complete formalization target. It does not accept an implementation, claim Lean elaboration, freeze the statements, or authorize proof construction by itself.

The reviewed author manifest is `7df9990e5e5f944f4c90890a6e12cd0cc6b9838cbbde2ee33a9f5061d7516c38`. The critical reviewed bytes are:

- Definitions: `cda485c546a5879f35eb9d69e7aaa7eaf1449d2219a47b75fcbb4c2827031988`.
- Challenge: `5b6b9a4c714686d6b060589a4ee1414f2ec94e80787a9ffe351ac6fe66331e57`.
- Numerical targets: `a95e32b7b8e654185f1d6bbbb3c483fa9b988e2ee5800a2ce641a230706e1cb9`.

`INPUTS.json` binds every retained draft, source, configuration and API file. The original canonical README, complete retained manuscript and earlier informal review were compared directly with Git `ce47b5630bf3680d9211131c3a43825b022c139a`, including the recorded blob identities. I read the actual target and manuscript independently; the earlier review's PASS and the author's finite check results were not used as proof evidence.

## Full target and nonvacuous semantics

The canonical question concerns every unequal pair of natural bandwidths, all nonsingular complex matrices of every order at least `1 + max p q`, and every maximal-modulus GEPP pivot choice in the given column/input ordering. `growthValues` has exactly these existential realizations and no finite dimension cap. `sharpConstant` is independently defined as its real supremum. It is not defined to be the proposed answer. The final `sharp_growth` requires both actual greatest-element membership and its supremum equality; the attaining witness and universal inequality therefore establish the least universal bound, rather than merely a conditional reduction.

The model starts at the literal original matrix. Each path entry is a current physical row; `rowSwap` swaps that row with the active diagonal position, and `schurStep` implements the precise complex quotient and subtraction on the trailing block. Zeros outside that block are padding, not discarded active entries. `AdmissiblePivot` requires a nonzero pivot, an active row, and only the maximum-modulus inequality. Equal moduli may be resolved arbitrarily. Neither a fixed tie rule, a real-only restriction nor a hidden preliminary reordering appears.

The finite NNReal suprema are genuine maxima of complex moduli. I inspected pinned `Finset.sup` and its maximum/attainment API, as well as the real supremum and greatest-element meanings. The positive dimension or a `Fin n` index makes each relevant maximum range nonempty. The numerator includes stage zero and every subsequent active stage through the final scalar. `entryMax_semantics`, `activeMax_semantics` and `growth_semantics` explicitly require attainment and the correct input scale. The growth semantics intentionally need no admissibility hypothesis: for any path the expression is a finite maximum containing the nonsingular input stage. This stronger bookkeeping assertion is valid and does not weaken later legal-path requirements.

`admissiblePath_exists` and `admissiblePrefix_extension` prevent a vacuous all-path conclusion. The latter must construct a genuinely admissible full path and preserve both the earlier choices and every state through the prefix. It is not a certificate assumed in a witness definition. The existence argument is supported mathematically by nonsingularity of successive Schur complements and finite maximal-pivot selection; it remains an actual proof obligation.

## General bandwidth front and recurrence

I checked `originalRow_update`, `multiplier_bounds`, `front_structure` and `front_transition` using the explicit physical-to-original permutation recursion. In zero-based stage `k`, every original row with label at least `k+p` has zeros in all preceding original columns. Such rows consequently had zero preceding multipliers, were not selected as earlier nonzero pivots, remain active, and retain their original active entries. The previous `k` distinct pivot labels were below `k+p`; thus at most `p` older rows survive. The chosen pivot lies among those rows and the next fresh row. At the following stage the old set is exactly this front with the actual pivot label removed. Missing bottom rows require no fictitious matrix entries: the finite sets simply contain fewer rows. This is a genuine general-`p` obligation; no two-row invariant from IE-14 is imported.

The structurally recursive history has zero initial slots, shifts every positive slot, and places one plus the previous first `p` slots in slot zero. `recurrence_history` and `sequence_recurrence` therefore express precisely the source's zero-at-nonpositive-times recurrence. Natural subtraction implements those negative source indices correctly because `h_p(0)=0`. At `p=0`, the sequence is zero at zero and one thereafter; its separately stated monotonicity and positivity remain valid. The positive-`p` envelope obligations correctly distinguish the zero state and first all-ones update, include `p=1`, and give the source's ordered update in every coordinate.

The finite-subset formulation of `column_front_bound` retains enough information for arbitrary pivots. To see why it matches the source's sorted envelope, order the front magnitudes as `y₁ ≥ … ≥ y_(p+1)`. Removing magnitude `y_a` leaves survivors bounded by `y_a+y_i`; their ordered values are bounded by `y₁+y₂,…,y₁+y_(p+1)`. More generally, the sum of any `r` updated survivors is at most the largest `r+1` front sum plus `(r−1)` times its largest entry. Bounds for every subset cardinality therefore propagate to the next canonical envelope, including appending a fresh entry of modulus at most the input scale. This uses the triangle inequality and multiplier moduli, so complex phases and every allowed pivot tie are covered. The envelope is a conclusion about actual rows, not an assumed invariant of an input class.

The zero-based `columnAge` is correct. For `j < p+q`, stage zero starts from the all-ones envelope after one imaginary update and stage `k` has age `k+1 ≤ j+1 ≤ p+q`. For `j ≥ p+q`, all old target entries vanish through stage `j−(p+q)`; the first possible fresh nonzero entry arrives there. The old-row age at stage `k` is consequently `max(0,k−(j−(p+q)))`, exactly the defined truncated subtraction. `late_column_zero` uses the inclusive endpoint at which old entries are still zero. Up to any active stage `k≤j`, the age is at most `p+q`. Fresh and untouched entries have their original input bound, while `h_p(p+q)≥1` for positive `p`. The universal all-active-entry bound consequently includes selected pivot rows and all other active entries, not just the final triangular factor.

For `p=0`, nonsingular banded inputs are upper triangular. Only the current diagonal row has a nonzero active pivot-column entry, lower multipliers vanish, and growth is exactly one. `zero_lower_bandwidth` and the identity witness explicitly retain this case. For `q=0` and positive `p`, all front, envelope and witness obligations remain present. The main theorem excludes only equal pairs as the canonical question does; several stronger helpers legitimately also cover those pairs.

## Exact original-order witness

I reconstructed the witness from the literal definitions and checked its zero-based translation against all of manuscript Section 4. Its order is `2p+q+1`, target column is `p+q`, and scale is the rational `(1/2)^p`. The lower factor has exactly the first `p` subdiagonals equal to minus one. The early upper vectors have first entry one and subsequent powers `2^(i−1)` in zero-based row `i`; later early columns are basis vectors. `witnessFactorIndex` is the inverse of the source rotation `(p+1,1,…,p,p+2,…)`. It assigns entries of the original input; it does not change the algorithm's starting state.

The geometric cancellations make the original early-column support lie between its diagonal and `p` rows below. Before scaling, each remaining magnitude is bounded by `2^p`. The target column begins at original row `p`, exactly `q` positions above its target index, and ends `p` rows below; later columns are literal identity columns. Thus both bandwidths, the maximum exactly one and rational-real embedding are required for the actual constructed matrix. The order satisfies the original union-of-dimensions lower bound for every parameter.

The seed path chooses current physical row `p` during the rotation, then the current diagonal. These are actual successive swaps. The explicit origin obligation is correct even at `q=0`: the first `p` swaps already complete the rotation; the possible subsequent swap at physical position `p` is the identity. At `p=0` the rotation is the identity as well. No claimed admissibility after the required prefix is assumed: `witness_prefix_admissible` proves exactly the first `p+q` steps and the separate extension theorem must complete them.

After preceding eliminations, each chosen early pivot column has the intended upper diagonal value times the lower-factor column, whose multipliers belong to `{0,−1}`. Hence these nonzero pivots are truly maximal in modulus, including ties. In the target column the permuted original data are `1`, then `p` zeros, then ones. Forward substitution yields the first value one and each subsequent value `h_p(i−1)` in one-based coordinates. The stage `p+q` diagonal is consequently `h_p(p+q)`, as required by `witness_target_value`. The nonzero leading pivots and later identity columns give the actual nonsingular whole matrix, asserted separately by `witness_nonsingular`. The universal bound and full prefix-preserving path then yield exact attainment, not just an asymptotic example or a conditional path.

`identity_attainment` supplies the missing `p=0` realization at dimension `q+1`. These two constructions prove nonemptiness and maximum membership of the actual growth set for every required unequal pair. Together with `universal_growth` they imply its greatest-element and real supremum statements. No optional closed-form specialization is needed to answer the original question completely.

## Independent exact diagnostics and source hygiene

I wrote and ran only the small standard-library exact-arithmetic diagnostic `check_exact.py`. It independently checks 35 rational witnesses with `1≤p≤5`, `0≤q≤6`: original band support, maximum one, a separate integer Bareiss determinant, prescribed prefix admissibility, rotation, actual target value, legal completion and exact growth. It also checks 56 rotations including both zero parameters, 3,500 recurrence-history slots and 6,368 sorted-front pivot cases.

For 61 additional small matrices it enumerates every legal maximum-modulus pivot path, using exact Gaussian-rational arithmetic. The resulting 266 full paths include 169 branching tie nodes. All active-entry bounds, original-row fronts, untouched rows, deletion transitions and late-column zeros are checked. For row-phase rational matrices it also checks the exact top-subset sums, which dominate every same-size subset; general Gaussian-rational cases use exact squared moduli for the growth and pivot comparisons. The identity cases include zero lower bandwidth. These are bounded diagnostics only and are not claimed to prove any general obligation or Lean acceptance.

The 13 reused generic semantic bodies were independently compared with the recorded immutable IE-14 source; they are byte-identical after isolating declarations. No IE-14 problem-specific theorem, cyclic witness or two-row invariant is imported. The pinned finite-maximum and supremum APIs support the chosen meanings. The recurrence and finite-subset approach avoid unnecessary interval or dimension enumeration inside Lean. Future LeanCert kernel trust assertions remain required; artificial interval work is unnecessary for this exact classification.

I independently validated the YAML against the actual pinned v0.4 JSON Schema using an existing parser environment, checked its 28 declarations against Comparator, and checked the toolchain/dependency pins. This is schema validation of a truthful statement draft, not a completed-proof promotion check. The original mathematical credit remains Matthew J. Colbrook, Cambridge DAMTP; the prospective formalization credit is George Stepaniants, Caltech Department of Computing and Mathematical Sciences, with no personal contact email. Source history, substantial AI assistance and the lack of human endorsement remain disclosed.

## Remaining gates

The mathematical definitions and all 28 contracts are approved at the exact bytes above. The scoped repository/Tau Ceti requirements for fidelity, generality, proof quality planning, reuse, naming, documentation and attribution have been applied. There was no author-source modification, local Lean/Lake execution, dependency download, proof implementation, inferred statement elaboration success, transitive-axiom audit or Comparator acceptance. A second independent statement approval, actual Linux elaboration and immutable freeze must precede proof implementation; the complete proof will then need its own independent final reviews and canonical mechanical gates.
