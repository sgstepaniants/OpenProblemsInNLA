# MI-04 independent continuation: diagonalization repair

**Approve the exact source repair for a fresh unchanged-limit Linux run.**
This is not runtime acceptance, a measured timeout improvement, Comparator
acceptance, or a completed-problem count change.

Reviewer: OpenAI Codex agent `/root/mf22_publication_referee`, 16 September
2026 UTC. I did not implement or propose these MI-04 edits and have not edited
the candidate, its frozen inputs or its Git checkout. The author is the
separate agent `/root/mi04_independent_referee`. I read the complete changed
Diagonalization module, exact patch, complete 534-line actual MI04 log, and
the relevant pinned primary declarations and their context. The applicable
scoped Tau Ceti fidelity, correctness, reuse and attribution criteria continue
the earlier complete-source review; this is not an official Tau Ceti service
or external human review.

The approved packet manifest is
`854186042727961b99ef33fad602a7190749c26c93ad8045234628edae0334fc`.
The complete candidate closure is
`d492f2a937f1c8495794baa31a87eb6844d4549a67f09285cf485dd323e02daa`.
The sole changed source is `NLA/MI04/Diagonalization.lean`, prospective SHA-256
`425dfa4d698ef5c259cf90c21dc90f710e02f92ce4d22e231d3c38b3a7072852`.

## Source-level assessment

Inside the existing `hAB` proof, the proposed `IsScalarTower` compatibility is
proved pointwise: scaling a continuous linear map before composition agrees
definitionally with scaling its value after composition. The proposed
`SMulCommClass` compatibility is exactly complex linearity of the existing
operator `S`, used in the symmetric direction. Neither instance is an added
hypothesis or a global declaration. Both are local proved propositions for
the existing actions. The pinned
`isStarNormal_iff_commute_realPart_imaginaryPart` requires precisely these
compatibilities, so its use now has the previously missing prerequisites.

The second change establishes the local `IsStarNormal X` instance directly
from the existing literal equation `Xᴴ * X = X * Xᴴ`. Matrix star is conjugate
transpose. The existing `Matrix.toEuclideanCLM` is an actual star algebra
equivalence, and the pinned `IsStarNormal.map` transports normality through
any multiplication/star-preserving map. This is the appropriate existing
theorem, with the same underlying operator `T`; no desired eigenbasis or
diagonalization conclusion is assumed.

The remainder of the complete changed module still recombines the simultaneous
eigenvectors of the self-adjoint real and imaginary parts, forms the actual
unitary basis matrix, proves its column equation and obtains the final matrix
identity from its unitary inverse. Arbitrary multiplicities, singular and zero
normal matrices, the helper's empty dimension and the public `1 ≤ n` scope are
preserved. No new simplicity, distinct-eigenvalue, invertibility or numerical
boundedness premise is introduced.

## Continuation from my complete review

I reconciled all 21 source files against my retained complete MI04 source
review and subsequent BasisTransport approval. Sixteen files are still exact
matches to that original complete review. BasisTransport matches my separately
approved three-body repair. I read the complete present Geometry, Normality
and JointBasis modules, as well as complete Diagonalization, to close the
intervening source delta rather than assuming it had already been reviewed
by me.

Geometry's extra scalar commutation normalization does not change its
coefficient identity. Normality's explicit star-map/coercion witnesses express
the same star algebra equivalence. JointBasis now separates its proof into
proved subordinate-basis, joint-space orthogonality and spanning helpers.
The orthogonality proof distinguishes the two actual eigenvalues and uses
the appropriate self-adjoint eigenspace theorem on actual vectors; spanning
still includes every joint eigenspace, with missing eigenvalues giving bottom.
The subordinate basis still spans each entire intersection, preserving
multiplicity and the whole ambient space. No helper assumes the joint-basis
conclusion.

The old `maxHeartbeats 800000` around JointBasis is present in the actual
before source and is unchanged by this repair. Diagonalization retains its
default 200,000 budget. This review does not describe the whole historical
development as having had no resource changes; it verifies that the current
repair changes none.

## Actual evidence and independent checks

Actual development run `35083895041` at literal commit
`09f9c4fd10c6e7d1efdb60eb0f43bc33ed920833` failed in Diagonalization. The missing
two instances, default-limit timeout, failed manual map bridge and downstream
rejected self-axiom are all in the actual log. JointBasis, Geometry and Normality
built, and the displayed transitive axioms of those exports were the configured
foundational three. This is progress within a failed development run, not a
successful whole-problem receipt. Comparator was not run.

I independently extracted all 21 before sources and the frozen Challenge from
the literal Git commit, including the explicit development mapping
`Solution.lean → NLA/MI04/Complete.lean`. Every byte matches its receipt entry
and all 13 command postmaps. The module log matches both the retained packet
copy and the receipt digest. All six primary Mathlib files independently match
their exact Git blobs at `0df444a360eaa60ab8c11dca51a86af692955474`.

The successful static audit rehashes every packet manifest item, all ten frozen
inputs, all 21 prospective sources, all 168 unchanged declaration headers and
all 21 unchanged public exports. It checks exact before/after import, option,
scope, axiom-print and trust-command lines; no new unsafe/native shortcut,
axiom or proof hole is present. The development checker, selector and pins
match literal Git and all postmaps. Twenty files are byte-identical across
this narrow repair. Original mathematical authorship remains Matthew J.
Colbrook, Cambridge DAMTP; formalization remains George Stepaniants, Caltech
Department of Computing and Mathematical Sciences, with AI assistance and no
new email disclosure.

One initial static audit attempt used a name matcher restricted to `theorem`
while an implementation contract is declared as `lemma`. That audit stopped
at its assertion and was not presented as successful. Its exact script and
explanation are retained under `preparation-history/`; the corrected matcher
accepts the two equivalent declaration keywords and preserves the independent
before/after header comparison. The final static audit exits successfully.

I ran no local Lean, Lake or Comparator. The explicit instances and transport
remove the reported source-level defects, but only actual execution can show
whether the declaration now fits its existing heartbeat budget and whether
downstream modules elaborate. Full Linux default-kernel, axioms, Comparator,
controls, publication and count gates remain pending.
